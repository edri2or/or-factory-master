# doc-binding exemptions — artifacts that are intentionally NOT bound to any doc.
#
# One artifact path (or basename) per line; lines starting with '#' are ignored.
# Matched by BASENAME (twin of monitoring/capability-card-exempt.txt), so a
# factory-pathed entry also covers a system-pathed copy. Read by
# scripts/check-doc-binding.sh. Full reference: docs/doc-drift-prevention.md.
#
# Keep this list SHORT and reviewed — every entry is a conscious decision that a
# change to the artifact never needs a documentation change. (Empty in v1: the
# only bound artifact is the named-queries workflow, and it stays bound.)
