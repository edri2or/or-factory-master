---
dev_name: הפחתת צריכת דקות GitHub Actions
slug: reduce-actions-minutes
opened: 2026-06-09
status: active   # active בזמן פיתוח → completed בסיום (משחרר את שער ה-CI)
---

# תוכנית פיתוח — הפחתת צריכת דקות GitHub Actions

## מטרה

חקירה גילתה שחיוב של $215.55 מ-GitHub במאי 2026 (~30,000 דקות Actions, חריגה מעל
מכסת 3,000 הדקות החינמיות) נבע מספרינט האיחוד — ~70 מערכות-בדיקה חד-פעמיות שהוקמו
ונפרסו (חלקן עשרות פעמים). המטרה: להוסיף מעקות בטיחות שמונעים חריגה כזו בעתיד, ולייעל
את הדיפלוי כך שספרינט פיתוח כבד יצרוך פי-כמה פחות דקות — בלי לפגוע בתפקוד.

## שלבים

| # | כותרת השלב | סטטוס | קבצים מושפעים |
|---|---|---|---|
| 1 | מעקות בטיחות זולים (timeout + concurrency + תדירות audits) | completed | `.github/workflows/*.yml`, `templates/system/.github/workflows/*.yml`, golden |
| 2 | ייעול דיפלוי לבדיקות (דילוג Caddy/worker + קיצור המתנת SSL ב-reuse) | completed | `templates/system/.github/workflows/deploy-railway-cloudflare.yml`, golden |
| 3 | הוכחה חיה + קידום + פירוק | pending | מערכת-טסט חיה (reuse mode) |

> סטטוס לכל שלב: `pending` / `in-progress` / `completed`.
>
> **הוכחה בכל שלב:** שלבים 1–2 הם שינויי תצורת-CI שנבדקים בשערים הסטטיים + רינדור
> הזהב; ההוכחה ההתנהגותית החיה (פרוביזן+דיפלוי אמיתי רץ מהר יותר ותקין) היא שלב 3.

---

### שלב 1 — מעקות בטיחות זולים

**Acceptance:**
- [ ] `timeout-minutes` על המשימה ב-`provision-system.yml` ובתבנית `deploy-railway-cloudflare.yml` (תקרה קשיחה לכל ריצה).
- [ ] `concurrency: { group: ..., cancel-in-progress: true }` על וורקפלואי ה-CI (פקטורי + תבנית מערכת) כך ש-push חדש מבטל ריצה ישנה.
- [ ] הרחבת תדירות ה-audits הכבדים (`factory-health-audit`, `system-runtime-audit`) מ-כל-6-שעות ל-יומי.
- [ ] הזהב (`tests/golden/system/`) רוענן (`--update`) באותו PR; שערים סטטיים ירוקים.

**הוכחה תפקודית (באותו שלב):** תצורה — נבדק ע"י השערים הסטטיים (Playground tests + Changelog gates incl. golden-in-sync) ורינדור הזהב שמראה שה-`timeout-minutes`/`concurrency` נוכחים בתבנית. ההוכחה ההתנהגותית בשלב 3.

**הערת התקדמות אחרונה:** הושלם (2026-06-09). נוספו `timeout-minutes` (provision=30, deploy=45), `concurrency: cancel-in-progress` ל-9 וורקפלואי CI (5 פקטורי + 4 תבנית), והרחבת שני ה-audits ל-יומי. הזהב רוענן (MANIFEST עודכן ל-5 קבצי תבנית); אומת מקומית `check-system-golden.sh` ללא drift. ממתין לאישור שערי ה-CI על PR #348.

**שינוי תוכנית:** —

---

### שלב 2 — ייעול דיפלוי לבדיקות

**Acceptance:**
- [ ] בניית Caddy ו-queue-worker (שתי ההמתנות הארוכות, ~10 דק' כל אחת) מדולגות במצב reuse (מערכות-בדיקה), מאחורי דגל.
- [ ] תקרות המתנת ה-SSL/readiness מוקטנות (`seq 1 60` → `seq 1 30`) כדי שדיפלוי שנכשל לא ישרוף 30–40 דק'.
- [ ] התנהגות מערכות אמיתיות (לא-reuse) נשמרת ללא שינוי.
- [ ] הזהב רוענן; שערים סטטיים ירוקים.

**הוכחה תפקודית (באותו שלב):** תצורה — נבדק ע"י השערים הסטטיים + רינדור הזהב שמראה את הדגל וההתניה. ההוכחה ההתנהגותית (דיפלוי-בדיקה רץ מהר יותר ועדיין עולה תקין) בשלב 3.

**הערת התקדמות אחרונה:** הושלם (2026-06-09). נוסף שלב `Detect throwaway test system` שמחשב `SKIP_EDGE=true` כש-`GCP_PROJECT_ID` מתחיל ב-`factory-test-/v2-test-/or-test-` (מערכות אמיתיות לא נוגעות). 5 שלבי ה-Caddy + בניית ה-worker מדולגים במערכת-בדיקה (n8n שומר את הדומיין, `/healthz` עובד). בנוסף קוצרו שתי לולאות readiness לכל המערכות; כל לולאות התעודה/בנייה נשארו רחבות. הזהב רוענן (רק hash של deploy השתנה); yamllint+shellcheck נקיים. ממתין ל-CI ולהוכחה החיה בשלב 3.

**שינוי תוכנית:** —

---

### שלב 3 — הוכחה חיה + קידום + פירוק

**Acceptance:**
- [ ] **(עלות — דורש אישור Or)** הוקמה מערכת-טסט זרוקה ב-reuse mode (`shared_gcp_project=factory-test-25`, 0 מכסה).
- [ ] השינויים הוחלו עליה; הדיפלוי רץ בתוך התקרות החדשות ומדלג על Caddy/worker.
- [ ] אומת חי: המערכת עולה תקין (`probe_endpoint` / Telegram) למרות הקיצורים.
- [ ] נמדד שיפור הזמן מול דיפלוי קודם.
- [ ] קידום = מיזוג ל-main; פירוק המערכת הזרוקה (`decommission-test-system.yml`, ביוזמת Or).

**הוכחה תפקודית (באותו שלב):** הוכחה חיה — דיפלוי אמיתי על מערכת-טסט חיה שעולה תקין בזמן מקוצר; אימות בלתי-תלוי דרך `probe_endpoint`/Telegram.

**הערת התקדמות אחרונה:** הוכח חי (2026-06-09). הוקמה `factory-test-actmin`, נרשמה ה-App (2 הקלקות של Or), השינוי הוחל דרך prove-on-test-system (אחרי 2 תיקוני-אגב שנמצאו חי: `workflows:write` לטוקן + הגנת `set -u` ב-Summary). דיפלוי שלישי **ירוק מלא**: שלב Detect → `SKIP_EDGE=true`, 5 שלבי Caddy (13/14/15/19/20) **skipped**, Railway+SSL+owner עלו, Summary עבר. אימות עצמאי: `GET https://n8n-factory-test-actmin.or-infra.com/healthz` → **200 `{"status":"ok"}`** (n8n מגיש ישירות, ללא Caddy). נותר: מיזוג ל-main + פירוק המערכת (ממתין לאישור Or).

**שינוי תוכנית:** נדרש תיקון-אגב ל-`prove-on-test-system.yml` (הוספת `workflows:write` לבקשת הטוקן) כדי שאפשר יהיה בכלל להוכיח שינוי בקובץ workflow — נכלל באותו PR.

---

## מצב מערכת-הטסט (Teardown ledger)

> מתמלא בשלב 3 — מצב יחיד: `torn-down — <תאריך/session>` או `left-alive by user decision — <תאריך/session>`.

- <טרם הוקמה מערכת-טסט>

## יומן ל-Or (עברית)

> שורה פשוטה אחת לכל שלב שהסתיים — בשפה ש-Or מבין, בלי ז'רגון.

- שלב 1 הושלם — שמנו "תקרת זמן" קשיחה לכל ריצת הקמה/דיפלוי (שלא תרוץ שעות בטעות), ביטלנו ריצות-CI כפולות, והורדנו את הבדיקות האוטומטיות מ-4-פעמים-ביום ל-פעם-ביום. זה לבד מבטיח שהפיצוץ של מאי לא יחזור.
- שלב 2 הושלם — מערכות-בדיקה זרוקות ידלגו עכשיו על הבנייה הארוכה (Caddy) ועל המתנות מיותרות, כך שכל סבב בדיקה יהיה הרבה יותר זול בדקות. מערכות אמיתיות לא מושפעות בכלל. (עדיין צריך להוכיח את זה חי — שלב 3).
- שלב 3 הוכח חי — הקמנו מערכת-בדיקה אמיתית, ראינו שהיא מדלגת על הבנייה הכבדה ובכל זאת עולה בריאה (`/healthz` החזיר 200). תוך כדי גם תיקנו שני באגים קטנים שנמצאו רק חי. נשאר למזג ולפרק.
