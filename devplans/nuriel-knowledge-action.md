<!--
תיק-פיתוח חי (DEVPLAN) מנוהל ע"י /dev-stage. הזיכרון/המצפן של הסוכן — לא חומר קריאה ל-Or.
Or לא פותח אותו; הסוכן קורא ממנו ומסכם לו בעברית פשוטה לפי דרישה.
-->
---
dev_name: נוריאל — המנכ"ל של המערכת (knowledge + action-clean + skills-native)
slug: nuriel-knowledge-action
opened: 2026-06-18
status: active   # active בזמן פיתוח → completed בסיום (משחרר את שער ה-CI)
---

# תוכנית פיתוח — נוריאל — המנכ"ל של המערכת

## מטרה

לחזק את נוריאל (`edri2or/nuriel`) לפני שהוא הופך רשמית לסוכן היחיד ש-Or מדבר איתו, בשלושה
עקרונות: (1) **knowledge-complete** — יודע את מי לבקש מה (מפת-צוות), ידע עמוק על Or, ואיך מוביל
פיתוחים; לא הטמעת-ידע ולא שליפה-עצמית. (2) **action-clean** — מנכ"ל שלא עושה שום עבודה בעצמו
(כולל קריאה/ניתוח/חיפוש); רק מבין → מבקש מחייל → מקבל → מרכיב. (3) **skills-native** — הצוות עובד
דרך סקילים (`.claude/commands`), כולל פרוטוקול-מסירה בין-סוכני ותקשורת מול Or.

תפיסת-היסוד של Or: Or=הבעלים, נוריאל=המנכ"ל, הסוכנים=החיילים — וכולם יודעים את שרשרת-הפיקוד.

## שלבים

| # | כותרת השלב | סטטוס | קבצים מושפעים |
|---|---|---|---|
| 0 | פתיחת הפיתוח (devplan + changelog) | completed | `devplans/`, `changelog.d/` |
| 1 | מחקר + הוכחת-יכולת-סקילים (capability-first) | completed | `docs/capability-cards/agent-repo-skills.md`, broker runs |
| 2 | טיוטת הפרסונה + סט-הסקילים | pending | `docs/agent-specs/nuriel.md`, `docs/agent-specs/firstwave/*`, קבצי-סקיל |
| 3 | נחיתה על הריפוז החיים (refresh) | pending | `edri2or/nuriel` (+חיילים), ענף-זמני |
| 4 | הוכחה חיה מסשן נוריאל | pending | — (broker run + הוכחה) |
| 5 | סגירה | pending | `CLAUDE.md`, devplan |

> סטטוס לכל שלב: `pending` / `in-progress` / `completed`.
> **הוכחה בכל שלב:** שלב נסגר רק כשהוכח על קלט אמיתי באותו שלב. בנייה מלמטה-למעלה; החיבור החי
> (סשן נוריאל) הוא הלבנה האחרונה, לא כלי-הבדיקה הראשון.

---

### שלב 0 — פתיחת הפיתוח

**Acceptance:**
- [x] `devplans/nuriel-knowledge-action.md` נוצר (`status: active`)
- [x] fragment ל-changelog נוצר

**הוכחה תפקודית (באותו שלב):** תוכן בלבד (scaffolding-תיעוד). שער ה-devplan ירוק (הקובץ הזה משחרר אותו).

**הוכחת E2E (artifact):** לא-התנהגותי.

**הערת התקדמות אחרונה:** הושלם — תיק-הפיתוח החי נפתח עם 6 שלבים, capability-first מסומן לשלב 1.

**שינוי תוכנית:** —

---

### שלב 1 — מחקר + הוכחת-יכולת-סקילים (capability-first)

**Acceptance:**
- [ ] מחקר-web: best-practices למנכ"ל-אורקסטרייטור (מנתב-לא-מבצע), שרשרת-פיקוד בצוותי-סוכנים, והטמעת skills בצוות
- [ ] הוכחת-יכולת go/no-go: האם סשן ריפו-סוכן (connector) + worker headless טוענים+מפעילים סקיל
- [ ] dog-food: מחקר→נתן, תיעוד→ספי (broker runs מתועדים)

**הוכחה תפקודית (באותו שלב):** verdict go/no-go מתועד בכרטיס-יכולת לסקילים-בריפו-סוכן + run-ids
של ה-broker מ-dog-food. בלי go לסקיל-ב-worker — לא בונים סקילים על חיילים (רק על סשן נוריאל אם הוכח).

**הוכחת E2E (artifact):** לא-התנהגותי (אין שינוי בקבצי-התנהגות n8n).

**הערת התקדמות אחרונה:** הושלם.
**(א) מחקר-web** (מסונתז ל-7 כללים לפרסונה, מקורות ב-`agent-repo-skills.md`/הפרסונה):
(1) אתה מנתב — לעולם לא מייצר את התוצר; (2) דע את המפה לא את השיטות — הטמע רוסטר+כללי-ניתוב+איך-לדבר-עם-Or,
שלוף ידע חי ע"י האצלה לא מזיכרון (נמנע מידע-מתיישן; progressive disclosure של Skills); (3) תן לכל חייל
brief בן-4: יעד / פורמט-פלט / כלים-ומקורות / גבולות; (4) סקֵל מאמץ לפי מורכבות (1/מעט/הרבה) — נגד over-spawning;
(5) האצל כלפי-מטה בלבד — חיילים לא מתכננים-מחדש ולא מאצילים-חזרה; (6) שמור הקשר נקי — קבל מהחיילים תוצר
מסונתז לא dump גולמי; (7) שפה אחת לניתוב ולסקילים (description של Skill = חוזה-הניתוב). מקורות ראשיים:
Anthropic *Building Effective Agents* + *multi-agent research system* + *Agent Skills*; A2A Agent Cards; MS ISE *Coordinator patterns*.
**(ב) הוכחת-יכולת סקילים (go/no-go):** **go** לסשן נוריאל האינטראקטיבי (Claude Code טוען `.claude/commands` —
אותו מנגנון כמו הסשן הזה; live-confirm בשלב 4); **no-go** ל-worker ה-headless כפי-שהוא (`--allowedTools Read,Grep,Glob`
לא כולל `Skill`, ואין לו MCP שמיש) → הפרוטוקול-הבין-סוכני מיושם בצד נוריאל (סקיל `/delegate`) + בפרסונת-החייל,
לא כסקיל שהחייל מריץ. הרחבת-חיילים = שינוי עתידי נפרד. הכל ב-`docs/capability-cards/agent-repo-skills.md`.
**(ג) dog-food (broker חי):** שרשרת L1 מלאה — נתן (run `27792616149`) חקר → ספי (run `27792785963`) תיעד
לרשומת-6-בלוקים; שניהם success, requester=`nuriel`, פעלו ביכולתם האמיתית והיו ישרים על מגבלותיהם.

**שינוי תוכנית:** —

---

### שלב 2 — טיוטת הפרסונה + סט-הסקילים

**Acceptance:**
- [ ] `docs/agent-specs/nuriel.md` חדש (7 הסעיפים: Or עשיר / מנכ"ל+שרשרת-פיקוד / מפת-צוות / 3 כללים / סקילים+פרוטוקול / הובלת-פיתוח / ניקיון+אבטחה)
- [ ] קבצי-סקיל: מנכ"ל (`/lead-dev`,`/delegate`,`/report-to-or`) + פרוטוקול-מסירה + סקיל-תפקיד לחיילים
- [ ] תוספת שרשרת-פיקוד ל-`firstwave/{nachshon,natan,sapi}.md`

**הוכחה תפקודית (באותו שלב):** הקבצים קיימים ועקביים; עוצר לסקירת-Or לפני נחיתה חיה. תוכן בלבד.

**הוכחת E2E (artifact):** לא-התנהגותי.

**הערת התקדמות אחרונה:** —

**שינוי תוכנית:** —

---

### שלב 3 — נחיתה על הריפוז החיים (refresh)

**Acceptance:**
- [ ] `AGENTS.md` + הסקילים נחתו על `edri2or/nuriel` (refresh run success)
- [ ] (אם הוכח בשלב 1) חיילים מקבלים סקיל-תפקיד + שרשרת-פיקוד באותו דפוס
- [ ] אימות חי: התוכן החי תואם את הטיוטה

**הוכחה תפקודית (באותו שלב):** `get_file_contents(nuriel, AGENTS.md / .claude/commands/*)` = הפרסונה
והסקילים החדשים. ריצות-refresh success.

**הוכחת E2E (artifact):** לא-התנהגותי.

**הערת התקדמות אחרונה:** —

**שינוי תוכנית:** —

---

### שלב 4 — הוכחה חיה מסשן נוריאל

**Acceptance:**
- [ ] Or פותח סשן נוריאל; בקשה אמיתית
- [ ] נוריאל מפעיל סקיל-מנכ"ל, מבקש מחייל בפרוטוקול (לא קורא בעצמו), מקבל, מרכיב תשובה

**הוכחה תפקודית (באותו שלב):** broker run-id מאמת + תשובה מורכבת. דורש את Or (גבול אנושי).

**הוכחת E2E (artifact):** לא-התנהגותי.

**הערת התקדמות אחרונה:** —

**שינוי תוכנית:** —

---

### שלב 5 — סגירה

**Acceptance:**
- [ ] עדכון רוסטר ב-`CLAUDE.md` (מסגרת מנכ"ל/חיילים + skills-native) אם צריך
- [ ] `/dev-insights`
- [ ] `status: completed`; הרשומות תואמות מציאות

**הוכחה תפקודית (באותו שלב):** devplan סגור, כרטיסי-היכולת מאומתים. תוכן בלבד.

**הוכחת E2E (artifact):** לא-התנהגותי.

**הערת התקדמות אחרונה:** —

**שינוי תוכנית:** —

---

## יומן ל-Or (עברית)

> שורה פשוטה אחת לכל שלב שהסתיים.

- שלב 0 הושלם — פתחתי את תיק-הפיתוח. נוריאל יחוזק כ"מנכ"ל": יודע את מי לבקש מה, לא עושה כלום בעצמו, ועובד דרך סקילים.
- שלב 1 הושלם — חקרתי באינטרנט (אישש בדיוק את הגישה: מנכ"ל "דק", יודע את המפה ולא את השיטות), הוכחתי שסקילים יעבדו לנוריאל אבל לא לחיילים ה-headless, והרצתי את הצוות חי כמבחן (נתן חקר, ספי תיעד — שניהם עבדו יפה).
