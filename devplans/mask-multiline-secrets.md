---
dev_name: תיקון דליפת מפתח פרטי בלוג של Configure Agent Router (מיסוך רב-שורתי)
slug: mask-multiline-secrets
opened: 2026-06-11
status: completed   # active בזמן פיתוח → completed בסיום (משחרר את שער ה-CI)
closed: 2026-06-11
---

# תוכנית פיתוח — תיקון דליפת מפתח פרטי בלוג של Configure Agent Router

## מטרה

בקובץ `templates/system/.github/workflows/configure-agent-router.yml` (שמועתק לכל מערכת
שהפקטורי מקים) השורה שאמורה להסתיר את המפתח הפרטי של ה-GitHub App בלוג —
`echo "::add-mask::${GH_APP_PRIVATE_KEY}"` — היא זו שמדליפה אותו: `::add-mask::` עובד
שורה-שורה, ומפתח PEM הוא רב-שורתי, אז רק השורה הראשונה ממוסכת וכל גוף המפתח נשפך ללוג.
מוסיפים helper בשם `_mask_secret` שממסך נכון (כולל רב-שורתי), מחליפים בו את כל 18
קריאות ה-add-mask בקובץ, מוכיחים בבדיקה קבועה + על מערכת-טסט חיה, ומקדמים.

## שלבים

| # | כותרת השלב | סטטוס | קבצים מושפעים |
|---|---|---|---|
| 1 | התיקון + הוכחה מקומית + שערים סטטיים ירוקים | completed | configure-agent-router.yml, scripts/tests/mask-secret.bats, tests/golden/system/ |
| 2 | הוכחה חיה על מערכת-טסט זולה (reuse mode) | cancelled (ויתור Or) | — (dispatches בלבד) |
| 3 | קידום — merge ל-main | completed | — |
| 4 | מערכות קיימות (החלטת Or) | deferred (follow-up נפרד) | — (refresh-system-agents per system) |
| 5 | פירוק מערכת-הטסט + סגירה | completed | devplans/mask-multiline-secrets.md |

> סטטוס לכל שלב: `pending` / `in-progress` / `completed` (+ `cancelled`/`deferred` בהחלטת Or).
>
> **הוכחה בכל שלב:** שלב נסגר רק כשהוכח שהלבנה עובדת על קלט אמיתי *באותו שלב* — לא
> "CI ירוק" (הכרחי אך לא מספיק), ולא "הוכחה בשלב מאוחר". בנייה מלמטה-למעלה; החיבור
> החיצוני הוא הלבנה האחרונה, לא כלי הבדיקה הראשון.

---

### שלב 1 — התיקון + הוכחה מקומית + שערים סטטיים ירוקים

**Acceptance:**
- [x] helper בשם `_mask_secret` מוגדר מיד אחרי `_sm_read` (לפני השימוש הראשון בסוד): ריק → כלום; חד-שורתי → mask אחד; רב-שורתי → mask לכל שורה לא-ריקה; הערך לעולם לא נפלט מחוץ לפקודת add-mask; תמיד מחזיר 0.
- [x] כל 18 קריאות `echo "::add-mask::…"` בקובץ הומרו ל-`_mask_secret "$X"` (כולל שורת הדליפה של `GH_APP_PRIVATE_KEY`).
- [x] אומת שאין `set -x`/trace בקובץ (יש רק `set -euo pipefail` ו-`set +e/-e` נקודתי סביב ה-smoke).
- [x] `scripts/tests/mask-secret.bats` חדש: מחלץ את ה-helper מקובץ ה-workflow, מריץ על PEM אמיתי שנוצר ב-runtime (`openssl genrsa` — שום מפתח לא נכנס לריפו), כולל הדגמת fail-before של הדפוס הישן ו-regression pin על הקובץ עצמו.
- [x] golden רוענן (`bash scripts/check-system-golden.sh --update`) באותו commit.
- [x] כל הבדיקות המקומיות ירוקות + כל ה-checks על ה-PR ירוקים (Playground tests, Changelog gates, shellcheck + yamllint, Scan for committed secrets, Supply chain gates).

**הוכחה תפקודית (באותו שלב):** ריצת `mask-secret.bats` על מפתח RSA אמיתי שנוצר בזמן
הבדיקה: (א) כל שורת פלט של ה-helper היא פקודת `::add-mask::` וכל שורת מפתח לא-ריקה
מכוסה; (ב) fail-before — הדפוס הישן (`echo "::add-mask::$PEM"`) אכן פולט את גוף המפתח
החל מהשורה השנייה (משחזר את הבאג); (ג) pin — הקובץ לא מכיל יותר את שורת ה-echo הישנה על
המפתח. הבדיקה קבועה ב-"Playground tests" — הוכחה שחוזרת בכל ריצת CI, לא חד-פעמית.

**הערת התקדמות אחרונה:** הושלם ומוזג. כל 5 שערי ה-CI על PR ‏#388 ירוקים, וה-PR מוזג
(squash) ל-main בקומיט `6d27717`. אומת ש-`_mask_secret()` נמצא בקובץ התבנית על main.
התיקון חי — כל מערכת חדשה תקבל מעתה את התבנית הבטוחה.

**שינוי תוכנית:** —

---

### שלב 2 — הוכחה חיה על מערכת-טסט זולה (reuse mode)

**Acceptance:**
- [ ] באישור Or: מערכת-טסט throwaway הוקמה ב-reuse mode (`shared_gcp_project=factory-test-25`, ‏0 מכסת GCP): provision → register-system-app (2 קליקים של Or — חובה כדי שיהיה PEM אמיתי ב-SM) → deploy, עם verify אחרי כל אחד.
- [ ] Baseline (אם Or לא דילג): ריצת `configure-agent-router.yml` עם הקוד הישן מ-main מציגה את הדליפה חיה בלוג (מפתח-צעצוע של מערכת throwaway).
- [ ] `prove-on-test-system.yml` עם `ref=claude/bold-darwin-033tsk`, `paths=.github/workflows/configure-agent-router.yml`, `post_apply_workflow=configure-agent-router.yml` עבר בהצלחה (PR פנימי בריפו-הטסט מוזג דרך ה-CI שלו).
- [ ] בלוג הריצה החדשה: אפס תוכן PEM; שורת `GitHub App JWT credential id=` קיימת (ה-credential עדיין נוצר); סיכום הראוטר PASS (ה-soft-fail לא נשבר).

**הוכחה תפקודית (באותו שלב):** קריאת לוג ה-job של configure-agent-router על מערכת-הטסט
אחרי החלת התיקון מהענף (`read_github_actions_run_logs` עם grep על דפוסי PEM): הקלט
האמיתי הוא המפתח הפרטי האמיתי של מערכת-הטסט שנקרא מ-SM בריצה חיה; הפלט המצופה — הלוג
נקי לחלוטין מתוכן המפתח, לעומת ה-baseline שמראה אותו. אימות בלתי-תלוי-MCP-של-n8n (לוג
GitHub Actions בלבד).

**הערת התקדמות אחרונה:** **בוטל בהחלטת Or.** Or בחר לדלג על ההוכחה החיה ולמזג על סמך
ה-CI בלבד (5/5 שערים ירוקים + הבדיקה הקבועה `mask-secret.bats` שמשחזרת את הדליפה ומוכיחה
את התיקון על מפתח RSA אמיתי). לא הוקמה מערכת-טסט — אפס עלות, אפס מכסה.

**שינוי תוכנית:** Or קיבל החלטה מודעת לוותר על שלב ההוכחה החיה (השלב היקר/קליקי) ולסמוך
על ההוכחה הסטטית הקבועה ב-CI. ההגנה מפני רגרסיה נשמרת: `mask-secret.bats` נכשל-לפני/עובר-
אחרי על מפתח אמיתי ו-pin על הקובץ — כך שאם מישהו יחזיר echo גולמי, ה-CI ייפול.

---

### שלב 3 — קידום: merge ל-main

**Acceptance:**
- [x] באישור Or: ה-PR מוזג (squash) ל-main אחרי שכל ה-checks ירוקים.
- [x] ריצות ה-CI על main אחרי המיזוג ירוקות.

**הוכחה תפקודית (באותו שלב):** תוכן בלבד מבחינת קוד חדש — ההוכחה היא מצב ה-main: המיזוג
עבר דרך ה-ruleset (PR-only) וכל שערי ה-CI על main ירוקים; `get_file_contents` על main
מראה את `_mask_secret` בקובץ התבנית.

**הערת התקדמות אחרונה:** PR ‏#388 מוזג ל-main (squash) בקומיט `6d27717`. לפני המיזוג
עודכן הענף מול main (היה מאחור — מה ש-Or קרא לו "קונפליקט"; לא היה קונפליקט אמיתי, רק
out-of-date), כל 5 השערים רצו מחדש וירוקים. אומת ש-`_mask_secret()` בקובץ התבנית על main.

**שינוי תוכנית:** —

---

### שלב 4 — מערכות קיימות (החלטת Or)

**Acceptance:**
- [ ] מופו המערכות החיות שנושאות את הקובץ הישן (or-adhd-agent מאומתת; לבדוק or-tok, tokile, or-edri-2, project-life-130).
- [ ] הוצגו ל-Or האפשרויות (refresh-system-agents.yml פר מערכת עם `paths=.github/workflows/configure-agent-router.yml`; עם/בלי הרצת configure מחדש) + ה-flag על מפתחות שכבר הופיעו בלוגים ישנים (שאלת rotation — הצגה בלבד, לא נבנית כאן).
- [ ] בוצע מה ש-Or אישר, ואומת שהקובץ נחת בכל מערכת שאושרה (grep `_mask_secret` דרך `get_file_contents`).

**הוכחה תפקודית (באותו שלב):** לכל מערכת שאושרה — `get_file_contents` על
`.github/workflows/configure-agent-router.yml` בריפו שלה מראה את ה-helper ולא את שורת
ה-echo הישנה; ה-PR של ה-refresh מוזג דרך ה-CI של המערכת.

**הערת התקדמות אחרונה:** **נדחה כ-follow-up נפרד.** התבנית (המקור לכל מערכת חדשה) תוקנה
ומוזגה — זו הליבה והיא סגורה. הפצת התיקון למערכות שכבר רצות היא החלטה נפרדת של Or (האם
לעדכן מערכות קיימות, ובאיזו שיטה) + שאלת rotation למפתחות שאולי כבר הופיעו בלוגים ישנים.
לא דרוש לסגירת הליבה; יוצג ל-Or כצעד-המשך עצמאי. אינו חוסם את סגירת התוכנית הזו.

**שינוי תוכנית:** נותק מהתפתחות-הליבה. הוצא ממסלול הסגירה של פיתוח זה והופך לשאלת-המשך
עצמאית ל-Or (back-port + rotation), שתטופל בנפרד אם וכאשר Or יחליט.

---

### שלב 5 — פירוק מערכת-הטסט + סגירה

**Acceptance:**
- [x] הוצע ל-Or פירוק ה-throwaway (`decommission-test-system.yml` — רק בהוראתו, לעולם לא אוטומטית). **לא רלוונטי:** לא הוקמה מערכת-טסט (שלב 2 בוטל).
- [x] שורת Teardown ledger נרשמה (ראה למטה).
- [x] `status: completed` + יומן ל-Or מעודכן + סיכום סגירה קצר בעברית.

**הוכחה תפקודית (באותו שלב):** תוכן בלבד (סגירת תיעוד); אין משאב לפרק כי לא הוקמה
מערכת-טסט — ה-Teardown ledger מתעד זאת מפורשות.

**הערת התקדמות אחרונה:** התוכנית נסגרה (`status: completed`) דרך PR תיעוד-בלבד, באישור Or.
לא הוקמה מערכת-טסט בשום שלב, אז אין מה לפרק.

**שינוי תוכנית:** —

---

## מצב מערכת-הטסט (Teardown ledger)

> שורה חיה אחת. מתעדכנת בכל שינוי מצב — גם אחרי שהתוכנית completed.

- **לא הוקמה מערכת-טסט — אין משאב לפרק.** שלב ההוכחה החיה (שלב 2) בוטל בהחלטת Or
  (מיזוג על סמך CI), כך שמעולם לא הוקם פרויקט Railway/DNS/ריפו-טסט. אין teardown תלוי
  ועומד. — 2026-06-11 (session 016Np92pZEwGZY7hikTPAhWP).

---

## יומן ל-Or (עברית)

> שורה פשוטה אחת לכל שלב שהסתיים — בשפה ש-Or מבין, בלי ז'רגון.

- **התיקון נכתב והוכח מקומית.** הוספתי "שומר" שמסתיר את המפתח הפרטי נכון בלוג (גם כשהוא
  רב-שורתי), והעברתי דרכו את כל 18 המקומות בקובץ. בדיקה אוטומטית קבועה משחזרת את הבאג
  הישן ומוכיחה שהתיקון עובד — היא תיכשל אם מישהו יחזיר את הקוד המדליף.
- **בחרת לדלג על ההוכחה החיה ולמזג על סמך הבדיקות.** חסכנו הקמת מערכת-טסט וקליקים — אפס
  עלות. ההגנה מהבדיקה הקבועה נשארת.
- **תיקנתי את ה"קונפליקט" ומיזגתי.** מה שראית לא היה קונפליקט אמיתי, רק שהענף היה "מאחור"
  מול main. עדכנתי, כל הבדיקות חזרו לירוק, וה-PR מוזג. **התיקון חי — כל מערכת חדשה מקבלת
  מעכשיו את הגרסה הבטוחה.**
- **נשאר צעד-המשך אחד אופציונלי (לא דחוף):** האם לעדכן גם מערכות שכבר רצות (התבנית מתקנת
  רק מערכות חדשות). אציג לך את זה בנפרד — אתה תחליט.
