---
dev_name: תיקון פערי-תיעוד מול הקוד (README, רמזי-קלט, הערות, SKILL)
slug: doc-code-drift
opened: 2026-06-11
status: completed
---

# תוכנית פיתוח — תיקון פערי-תיעוד מול הקוד

## מטרה

חמישה מקומות בתיעוד/בהערות בריפו סטו ממקור-האמת בקוד: שני מספרים ב-README (מספר משתני-ה-repo
ומספר הסודות הגנריים) וכלל-שם-המערכת (`system_name`, ‎6–30 תווים בפועל) שמתואר לא-אחיד בארבעה
עותקים. מיישרים הכל מול הקוד — **תיעוד/הערות בלבד, אפס שינוי לוגיקה או התנהגות** — כדי שהתיעוד
יישאר אמין ושלא תהיה סתירה פנימית מטעה במפעיל.

## שלבים

| # | כותרת השלב | סטטוס | קבצים מושפעים |
|---|---|---|---|
| 1 | יישור חמשת פערי-התיעוד מול הקוד | completed | README.md · .github/workflows/provision-system.yml · scripts/copy-generic-secrets.sh · skills/build-system/SKILL.md |

> סטטוס לכל שלב: `pending` / `in-progress` / `completed`.

---

### שלב 1 — יישור חמשת פערי-התיעוד מול הקוד

**Acceptance:**
- [x] README אומר "5 repo variables" (תואם 5 קריאות `_set_var`) ולא טוען מספר-קבוע של generic secrets.
- [x] רמז-הקלט `system_name` ב-`provision-system.yml` אומר "6-30 chars" (תואם המינימום הנאכף ≥6).
- [x] ההערה ב-`copy-generic-secrets.sh` לא טוענת "same rule" אלא מבהירה בדיקת-צורה + ≥6 ב-workflow.
- [x] ה-regex ב-`build-system/SKILL.md` מסומן כצורה האפקטיבית של ‎6–30, עם הבהרת המימוש בקוד.

**הוכחה תפקודית (באותו שלב):** תוכן בלבד — אין התנהגות רצה. האימות הוא קריאת-עיניים מול מקור-האמת
בקוד (5 קריאות `_set_var`; בדיקת `-lt 6`; הבנייה הדינמית של `GENERIC_SECRETS` — נספרו 47 גנריים
חיים מתוך 53 ב-control SM, פחות 6 החרגות) + CI ירוק.

**הוכחת E2E (artifact):** לא-התנהגותי.

**הערת התקדמות אחרונה:** הושלם — חמשת התיקונים בוצעו ב-PR אחד עם fragment ל-changelog; אין נגיעה בלוגיקה.

**שינוי תוכנית:** —

---

## יומן ל-Or (עברית)

- שלב 1 הושלם — תיקנו חמישה פערי-תיעוד קטנים (שני מספרים ב-README ורמז כלל-השם בארבעה מקומות) כך
  שהתיעוד תואם את הקוד, בלי לגעת בשום לוגיקה. אגב, גילינו שגם ה-"40" שבהערה בקוד כבר מיושן (היום 47),
  ולכן ב-README העדפנו ניסוח דינמי בלי מספר.
- נסגר — ה-PR מוזג ל-main (#421) ב-12.06.2026; כל חמשת התיקונים חיים. התוכנית סומנה `completed`.
