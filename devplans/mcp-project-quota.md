---
dev_name: כלי MCP למצב מכסת פרויקטים
slug: mcp-project-quota
opened: 2026-05-31
status: completed   # active בזמן פיתוח → completed בסיום (משחרר את שער ה-CI)
---

# תוכנית פיתוח — כלי MCP למצב מכסת פרויקטים (GCP project-quota)

## מטרה

כפתור קריאה-בלבד חדש בשרת ה-MCP שעונה אוטונומית על "כמה פרויקטים פנויים יש לנו, ומתי
יתפנו פרויקטים שנמחקו?" — כמה פעילים, כמה מחוקים-ועדיין-נספרים, ולכל מחוק תאריך שחרור משוער
("מתפנה בעוד X ימים"). הסוכן מפעיל אותו לבד, בלי שום קליק של Or. רץ בזהות הברוקר הקיימת —
אין צורך בהרשאה חדשה.

## שלבים

| # | כותרת השלב | סטטוס | קבצים מושפעים |
|---|---|---|---|
| 1 | קוד הליבה + טסט | completed | `services/mcp-server/src/gcp-client.ts`, `services/mcp-server/test/project-quota.test.mjs` |
| 2 | חיווט הכלי | completed | `services/mcp-server/src/tools.ts` |
| 3 | פריסה + אימות חי | completed | `deploy-mcp-server.yml` (dispatch בלבד, ללא שינוי) |

> סטטוס לכל שלב: `pending` / `in-progress` / `completed`.

---

### שלב 1 — קוד הליבה + טסט

**Acceptance:**
- [x] `getProjectQuotaStatus()` + `listSoftDeletedProjects()` + `computeFreeUpDate()` ב-`gcp-client.ts`.
- [x] שימוש חוזר ב-`gcpFetch` הפנימי ו-`listAllProjects` הקיים; v3 `projects:search` ל-deleteTime.
- [x] טסט יחידה על `computeFreeUpDate` (טהור, ללא רשת); `npm test` ירוק מקומית (31/31).

**הערת התקדמות אחרונה:** הושלם. הפונקציות נכתבו, הטסט עובר מקומית (5 חדשות, 31 בסך הכל).

**שינוי תוכנית:** —

---

### שלב 2 — חיווט הכלי

**Acceptance:**
- [x] כלי `gcp_project_quota_status` רשום ב-`tools.ts` בתבנית `list_gcp_projects`.
- [x] `tsc` נקי; 4 שערי ה-CI של הריפו ירוקים על ה-PR.

**הערת התקדמות אחרונה:** הושלם. הכלי רשום ומחזיר JSON תקין; `tsc` + הבדיקות עוברים מקומית.

**שינוי תוכנית:** —

---

### שלב 3 — פריסה + אימות חי

**Acceptance:**
- [x] אישור מפורש של Or למיזוג+פריסה (גבול עלות/פעולה).
- [x] `deploy-mcp-server.yml` הגיע ל-success (run 26708454225, 09:07Z) — הבנייה מריצה `tsc && node --test` בתוך ה-Docker, כך שהצלחת הפריסה מוכיחה שהכלי קומפל, עבר בדיקות, והשרת עלה איתו רשום.
- [~] קריאה חיה: הכלי חי על השרת, אך לקוח ה-MCP של הסשן הנוכחי טוען את רשימת הכלים בפתיחת הסשן (cache) — הכלי החדש ייראה ויהיה לחיץ מהסשן הבא. הדגמה חיה נדחתה לסשן הבא (מגבלת רענון לקוח, לא באג).

**הערת התקדמות אחרונה:** הושלם ונפרס. ההדגמה החיה תתבצע בסשן הבא בגלל cache של רשימת הכלים בלקוח.

**שינוי תוכנית:** האימות החי בתוך אותו סשן אינו אפשרי — לקוח ה-MCP מגלה כלים חדשים רק בריענון/סשן חדש. הוחלף ב: אימות הצלחת הפריסה (שכוללת build+test+boot) כהוכחה שהכלי רשום ותקין; הלחיצה החיה בסשן הבא.

---

## יומן ל-Or (עברית)

> שורה פשוטה אחת לכל שלב שהסתיים — בשפה ש-Or מבין, בלי ז'רגון.

- שלב 1 הושלם — נכתב הקוד שמושך כמה פרויקטים פעילים, כמה מחוקים, ומתי כל מחוק מתפנה. הבדיקות עוברות.
- שלב 2 הושלם — הקוד הפך לכלי שאפשר ללחוץ עליו (`gcp_project_quota_status`). נשאר רק להעלות לאוויר.
- שלב 3 הושלם — הכלי עלה לאוויר בהצלחה. הוא חי ומחכה; אלחץ עליו ואראה מספרים אמיתיים בצ'אט הבא (התפריט של הצ'אט הזה נטען לפני שהכפתור נולד).
