---
dev_name: הקשחה נגד הפניות למשאבים שהוצאו משימוש (תובנות post-mortem)
slug: harden-retired-ref-guards
opened: 2026-06-17
status: completed   # נסגר 2026-06-17 — 3 התובנות מ-/dev-insights מומשו ואומתו; ראו הערת סגירה
---

# תוכנית פיתוח — הקשחה נגד הפניות למשאבים שהוצאו משימוש

## מטרה

אחרי הסרת הסקיל המת `gcp-hands-client` (PR #506), ‏`/dev-insights` העלה 3 תובנות מוכחות
ש-Or אישר. הן מקשיחות את הפקטורי נגד *המחלקה* של הבאג שקרה (ארטיפקט שנשלח למערכות ומפנה
למשאב שכבר לא קיים), ועוד papercut תשתיתי שנתקלנו בו. פיתוח `/dev-stage` רגיל (צד-פקטורי:
סקריפט CI, CLAUDE.md, hook פקטורי-בלבד) — לא משנה מה הפקטורי מקצה, אז לא `/dev-stage-factory`.

## שלבים

| # | כותרת השלב | סטטוס | קבצים מושפעים |
|---|---|---|---|
| 1 | שער דנילִיסט (D) למזהים-שהוצאו-משימוש + טסט | completed | `scripts/check-golden-sync.sh`, `scripts/tests/check-golden-sync.bats` |
| 2 | כלל ב-CLAUDE.md: סקיל-תבנית ≠ יכולת-פקטורי | completed | `CLAUDE.md` |
| 3 | הקשחת `session-start.sh` (envsubst/apt) | completed | `.claude/hooks/session-start.sh` |

> **הערת סגירה (2026-06-17):** 3 התובנות מומשו ואומתו מקומית (השער החדש עובר נקי בלי
> false-positive על `edri2or/factory`; הטסט החדש ירוק; shellcheck נקי). שלב 3 בביטחון
> בינוני — יש רכיב סביבתי לא-מוסבר (ה-hook הדפיס "ready" באתחול למרות ש-envsubst חסר,
> כנראה ארעיוּת-קונטיינר) — אז זו הקשחת-עמידוּת + דיווח-אמת, לא תיקון-שורש מוכח.

---

### שלב 1 — שער דנילִיסט (D)

**Acceptance:**
- [x] `check-golden-sync.sh` סעיף (D) נכשל אם `edri2or/gcp-hands` / `gcp-hands-control` / `factory-control-9piybr` מופיעים תחת `templates/system/`.
- [x] `edri2or/factory` **לא** ברשימה (6 הערות-מקור לגיטימיות ב-deploy template).
- [x] טסט bats חדש מוכיח את הכישלון; `check-golden-sync.sh` עדיין עובר נקי על הריפו האמיתי.

**הוכחה תפקודית (באותו שלב):** `bash scripts/check-golden-sync.sh` → PASS; `bats scripts/tests/check-golden-sync.bats` → כל המקרים ירוקים כולל החדש. ✅

**הוכחת E2E (artifact):** לא-התנהגותי.

**הערת התקדמות אחרונה:** הושלם.

**שינוי תוכנית:** —

---

### שלב 2 — כלל CLAUDE.md

**Acceptance:**
- [x] פסקה חדשה באזור "Skills audience" שמבהירה: סקיל שצף מ-`templates/system/.claude/skills/` הוא ארטיפקט-נשלח-למערכות, לא יכולת-פקטורי — אל תצטט כתקדים.

**הוכחה תפקודית (באותו שלב):** תוכן בלבד.

**הוכחת E2E (artifact):** לא-התנהגותי.

**הערת התקדמות אחרונה:** הושלם.

**שינוי תוכנית:** —

---

### שלב 3 — הקשחת ה-hook

**Acceptance:**
- [x] `apt-get update` כושל כבר לא מדלג על התקנת `gettext-base` (‏`|| true` + install עם `|| true`).
- [x] בלוק verify-then-report בסוף ה-hook: בודק shellcheck/yamllint/envsubst/bats ומדפיס `::warning::` על חוסר, במקום "ready" עיוור. לא-פטאלי.
- [x] ההערה ב-hook עודכנה (הטענה ש"בסיס-apt עובד" הוחלפה באמת על ה-PPA-ים השבורים).

**הוכחה תפקודית (באותו שלב):** `shellcheck .claude/hooks/session-start.sh` נקי. (אימות מלא דורש את סביבת-הווב; זו הקשחת-עמידוּת.)

**הוכחת E2E (artifact):** לא-התנהגותי.

**הערת התקדמות אחרונה:** הושלם, בביטחון בינוני (ראו הערת סגירה).

**שינוי תוכנית:** —

---

## יומן ל-Or (עברית)

> שורה פשוטה אחת לכל שלב שהסתיים — בשפה ש-Or מבין, בלי ז'רגון.

- שלב 1 הושלם — הוספנו "שומר סף" ש-CI שייכשל אם מערכת חדשה תפנה שוב לריפו/פרויקט מת.
- שלב 2 הושלם — כתבנו כלל ב-CLAUDE.md שמסביר לסוכנים עתידיים לא להתבלבל מסקיל-תבנית.
- שלב 3 הושלם — תיקנו את ה-hook שאמר "מוכן" גם כשכלי חסר; עכשיו הוא אומר את האמת.
