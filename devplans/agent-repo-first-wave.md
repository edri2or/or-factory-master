<!--
DEVPLAN — agent-repo-first-wave
מנוהל על-ידי /dev-stage. הקובץ הוא הזיכרון/המצפן של הסוכן (לא חומר קריאה ל-Or).
Or לא פותח אותו; הסוכן קורא ממנו ומסכם לו בעברית פשוטה לפי דרישה.
-->
---
dev_name: הגל הראשון — נחשון (נתב) + נתן (מחקר) + ספי (תיעוד)
slug: agent-repo-first-wave
opened: 2026-06-17
status: active   # active בזמן פיתוח → completed בסיום (משחרר את שער ה-CI)
---

# תוכנית פיתוח — הגל הראשון של ריפו-סוכנים

## מטרה

לייצר 3 ריפו-סוכנים אמיתיים מעל תשתית ה"ריפו-סוכן" המוכחת, ולחבר אותם דרך ה-broker:
**נחשון** (נתב — מקבל בקשה, מפצל, שולח לאחרים, מאחד תוצאה), **נתן** (מחקר/תכנון),
**ספי** (תיעוד/מסמכים). היכולת החדשה היא ה-**fan-out של נחשון** — עובד שמצהיר על תת-עבודה
שנשלחת לסוכן אחר דרך ה-broker — ומוכחת לבד (capability-first) לפני בנייה.

> **יש יכולת חדשה** — ה-fan-out של נחשון. לכן Step 0 של capability-first **לא מדולג**:
> שלב 1 (spike זרוק) הוא הוכחת-הלבנה הקשה, עם go/no-go ב-`docs/capability-cards/firstwave-fanout.md`,
> לפני שמוציאים שקל על ריפויי-אמת. **גבול אבטחה מעצב:** העובד הוא קריאה-בלבד ואין לו מפתח
> ל-broker, לכן הוא לא שולח בעצמו — הוא **מצהיר** על תוכנית-פיצול, ובעל-הרשאה (בגרסה 1: סשן
> הפקטורי / קלוד צעד-צעד) שולח דרך ה-broker הקיים, אוסף ומאחד. ה-workflow האוטונומי
> (`agent-fanout.yml`) נדחה לפיתוח נפרד עם הוכחת-יכולת + סקירת-RED + אישור-עלות משלו.
>
> **עטיפה: `/dev-stage` (לא factory).** לא נוגעים ב-`templates/system/**`, `provision-system.yml`,
> ולא בקבצי-התנהגות n8n — שער ה-E2E על or-edri-4 הוא no-op כאן. אין שינוי קוד-מוצר ב-MVP:
> רוכבים על `agent-action.yml` / `provision-agent-repo.yml` / `refresh-agent-repo.yml` הקיימים.

## שלבים

| # | כותרת השלב | סטטוס | קבצים מושפעים |
|---|---|---|---|
| 0 | פתיחת תיק (devplan + changelog + כרטיס-יכולת) | in-progress | `devplans/`, `changelog.d/`, `docs/capability-cards/firstwave-fanout.md` |
| 1 | הוכחת יכולת ה-fan-out (spike זרוק על ריפויי zz-) | pending | `spikes/firstwave-fanout/` |
| 2 | פרובוז 3 הריפויים האמיתיים ★גבול-עלות★ | pending | (דיספוז `provision-agent-repo.yml` ×3) |
| 3 | הזרקת פרסונת AGENTS.md לכל ריפו חי | pending | `docs/agent-specs/firstwave/`, (refresh חי) |
| 4 | כל סוכן עונה לבד + לולאת-broker (Gate 1) | pending | (לולאות `agent-action.yml`) |
| 5 | fan-out + unify על השלושה (Gate 2 — לבנה אחרונה) | pending | (לולאות `agent-action.yml`) |

> סטטוס לכל שלב: `pending` / `in-progress` / `completed`.
>
> **הוכחה בכל שלב:** שלב נסגר רק כשהוכח שהלבנה עובדת על קלט אמיתי *באותו שלב*. בנייה
> מלמטה-למעלה; ה-fan-out הוא הלבנה האחרונה, לא כלי הבדיקה הראשון.

---

### שלב 0 — פתיחת תיק

**Acceptance:**
- [x] `devplans/agent-repo-first-wave.md` נוצר (`status: active`).
- [x] `changelog.d/2026-06-17-agent-repo-first-wave.md` נוצר.
- [x] `docs/capability-cards/firstwave-fanout.md` נוצר (`verdict: pending`).

**הוכחה תפקודית (באותו שלב):** תוכן בלבד — קובצי-md לפתיחת התיק, אין התנהגות רצה.

**הוכחת E2E (artifact):** לא-התנהגותי.

**הערת התקדמות אחרונה:** התיק נפתח; ממתין לאישור אור להתחיל את שלב 1 (ה-spike).

**שינוי תוכנית:** —

---

### שלב 1 — הוכחת יכולת ה-fan-out (Phase 1, spike זרוק)

**Acceptance:**
- [ ] `spikes/firstwave-fanout/` עם prompt נחשון-נתב זרוק + README runbook.
- [ ] הרצה חיה דרך ה-broker על 3 ריפויי `zz-` קיימים: SPLIT → fan-out → איסוף → UNIFY.
- [ ] הכרעת go/no-go נרשמה ב-`docs/capability-cards/firstwave-fanout.md` עם run-ids.

**הוכחה תפקודית (באותו שלב):** קלט אמיתי דו-תחומי (לדוגמה: "מצא 3 שיטות עבודה לכתיבת
תיעוד טכני והכן מהן צ'קליסט"). מעבר-1 פולט JSON עם `plan[]` מוגבל ל-allow-list האחים;
שתי תת-המשימות הוחזרו תקין כקבצי `results/<corr>-<sub>.json`; מעבר-2 מסנתז **באמת** את
שתי התוצאות (אימות-יד). אף עובד לא קיבל הרשאה חדשה. GO רק אם כל אלה מתקיימים.

**הוכחת E2E (artifact):** לא-התנהגותי (ריפו-סוכן אין לו inbound-Telegram; ההוכחה היא
כרטיס-היכולת — תקדים `agent-broker-handoff.md`).

**הערת התקדמות אחרונה:** —

**שינוי תוכנית:** —

---

### שלב 2 — פרובוז 3 הריפויים האמיתיים  ★ גבול-עלות אמיתי — עצירה לאישור אור ★

**Acceptance:**
- [ ] `provision-agent-repo.yml` הורץ ×3: `nachshon`, `natan-research`, `sapi-docs`.
- [ ] כל ריפו נולד פרטי עם 4 הקבצים + קישור לדלת-WIF.

**הוכחה תפקודית (באותו שלב):** `get_repo` מאשר ריפו פרטי + `get_file_contents` מאשר 4
קבצים; לולאת-broker "ערומה" אחת לכל ריפו מחזירה `results/<corr>.json` תקין. run-ids נרשמים.

**הוכחת E2E (artifact):** לא-התנהגותי.

**הערת התקדמות אחרונה:** —

**שינוי תוכנית:** —

---

### שלב 3 — הזרקת פרסונת AGENTS.md לכל ריפו חי

**Acceptance:**
- [ ] תוכן ה-Drive נקרא (אחרי אימות Google) וצוקק ל-`docs/agent-specs/firstwave/{nachshon,natan,sapi}.md`.
- [ ] כל פרסונה הוחלה על הריפו החי דרך `refresh-agent-repo.yml`; נחשון מקבל גם את חוזה-ההצהרה + allow-list האחים.

**הוכחה תפקודית (באותו שלב):** `get_file_contents` על כל `AGENTS.md` חי מראה שהפרסונה נחתה
(נחשון: כולל ההוראה "אל תענה — פצל ופלוט הצהרה").

**הוכחת E2E (artifact):** לא-התנהגותי.

**הערת התקדמות אחרונה:** —

**שינוי תוכנית:** —

---

### שלב 4 — כל סוכן עונה לבד + לולאת-broker (Gate 1)

**Acceptance:**
- [ ] נתן: שאלת-מחקר אחת → תשובת מחקר/תכנון מהותית בקובץ-תוצאה.
- [ ] ספי: שאלת-תיעוד אחת → תשובה בצורת-תיעוד.
- [ ] נחשון חד-תחומי: `mode:"single"` תשובה ישירה.

**הוכחה תפקודית (באותו שלב):** שלוש לולאות-broker עצמאיות; כל קובץ-תוצאה מאומת-יד; לכל
ריפו ≥ הרצת `agent-action.yml` ירוקה אחת + אירועי-audit. ללא fan-out.

**הוכחת E2E (artifact):** לא-התנהגותי.

**הערת התקדמות אחרונה:** —

**שינוי תוכנית:** —

---

### שלב 5 — fan-out + unify על השלושה (Gate 2 — הלבנה האחרונה)

**Acceptance:**
- [ ] בקשה דו-תחומית אמיתית: SPLIT (נחשון) → fan-out (נתן+ספי) → איסוף → UNIFY (נחשון).
- [ ] שרשרת-correlation מלאה: `<corr>.json`, `<corr>-a.json`, `<corr>-b.json`, `<corr>-final.json`.

**הוכחה תפקודית (באותו שלב):** התשובה הסופית נשענת **באמת** על נתן וגם ספי (אימות-יד);
כל run-ids + אירועי-audit נרשמים. זו ההוכחה-הכותרת של הגל הראשון.

**הוכחת E2E (artifact):** לא-התנהגותי.

**הערת התקדמות אחרונה:** —

**שינוי תוכנית:** —

---

## יומן ל-Or (עברית)

> שורה פשוטה אחת לכל שלב שהסתיים — בשפה ש-Or מבין, בלי ז'רגון.

- שלב 0 הושלם — פתחתי את תיק-הפיתוח של "הגל הראשון": מטרה, שלבים, וכרטיס להוכחת היכולת החדשה (ה-fan-out).
