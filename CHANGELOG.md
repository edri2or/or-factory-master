# Changelog

## Stage 118 — feat: OIL auto-fix Phase 1 / PR 2 — the Linear "bell" (webhook → rules triage → repository_dispatch)

| PR | Type | Summary |
|---|---|---|
| TBD | feat | Second PR of the OIL auto-fix loop — the inbound "bell" that turns a newly-opened `OIL-*` Linear issue into an automatic, read-only investigation. New `services/mcp-server/src/oil-autofix.ts`: `verifyLinearSignature` (constant-time HMAC-SHA256 over the RAW request body vs the `Linear-Signature` header), `extractOtel` (pulls the OTel JSON the observability pipeline embeds in the issue body — same fenced-block contract as PR 1), `triage` (deterministic rules applied IN ORDER so `factory.pilot.test`→test-event and `severity:info`→info-maintenance fire **before** `action_required` — exactly the OIL-12/OIL-13 noise cases — then `action_required:false`→skip, else dispatch), `handleLinearWebhook` (503 if disabled, 401 on bad signature, replay-guard on `webhookTimestamp`, only `action:create`+`type:Issue`, then triage → `repository_dispatch(oil-investigate)`), and `registerLinearWebhook` (idempotent Linear `webhookCreate` → `<mcp>/linear-webhook`). `index.ts` adds a raw-body capture to `express.json({verify})` and registers `POST /linear-webhook` (always 2xx unless the signature is bad, so Linear never retry-storms / auto-disables) + admin-gated `POST /oil-register-webhook`. `github-client.ts` gains `dispatchRepositoryEvent` (mirrors `dispatchWorkflow` — same broker-App `ghFetchRepo` auth, POSTs `/dispatches`, expects 204). `observability-client.ts` exports `readSecretSoft` + `linearGql` for reuse. `tools.ts` adds `oil-autofix-investigate.yml` to the `dispatch_workflow` allowlist (lets the agent run an on-demand verification; the live loop triggers via repository_dispatch). `deploy-mcp-server.yml` mints + mounts `LINEAR_WEBHOOK_SECRET=linear-webhook-secret:latest` (broker SA already holds project-level secretAccessor — no new IAM). The loop's own events (`factory.oil_autofix.{dispatched,skipped,dispatch_failed}`) are emitted soft-fail and **non-actionable** (info/warning, `action_required:false`) so it can never open a Linear issue about itself and re-trigger. Detection keys on the OTel-JSON signature (not the `auto-created` label, which Linear's payload doesn't reliably carry by name; label honoured when present). **Scope kept focused** vs the original plan: the reconciler backstop is split to a separate follow-up PR; Haiku triage is deferred (the investigator classifies deeply; rules suffice to drop noise); the payload JWT is deferred (repository_dispatch needs privileged creds + the investigator is read-only → low risk). DEVPLAN stage 1 → completed, stage 2 → in-progress. **Activation is one-time and operator-gated** (not in this PR's merge): redeploy the MCP via `deploy-mcp-server.yml` (self-mints the secret), then `POST /oil-register-webhook`, then live-verify by dispatching the investigator on OIL-16 + a benign test failure end-to-end — this also closes PR 1's deferred live check. Verified pre-merge: `tsc` typecheck clean; gate scripts + yamllint green. |

## Stage 117 — feat: OIL auto-fix Phase 1 / PR 1 — read-only investigator workflow + DEVPLAN opened

| PR | Type | Summary |
|---|---|---|
| TBD | feat | First PR of the OIL auto-fix loop — the "second half" of the observability pipeline (today it detects failures and opens `OIL-*` Linear issues, but nothing reads and fixes them). New `.github/workflows/oil-autofix-investigate.yml`: triggers on `repository_dispatch:[oil-investigate]` (the MCP server will fire this after triage in a later stage) and on manual `workflow_dispatch(issue_id)` for testing. It authenticates to the control project via the existing broker WIF chain (same pattern as `observability-pilot.yml`/`factory-health-audit.yml`), reads the target Linear issue (the `id` field accepts the `OIL-NN` identifier as well as the UUID), extracts the OTel JSON the pipeline wrote into the issue body, derives `factory.run_id`/`factory.layer`/`factory.system_name`, and best-effort fetches that run's logs with a tightly-scoped broker App token (`actions:read` on just the target repo — factory-layer runs in `or-factory-master`, system-layer runs in the system's own repo) via `scripts/generate-app-token.sh`. It then runs `anthropics/claude-code-action` pinned to the v1 commit SHA `787c5a0c` in strictly READ-ONLY mode (`--allowedTools Read,Grep,Glob --disallowedTools Bash,Edit,Write,MultiEdit,NotebookEdit,WebFetch,WebSearch`, `--model claude-sonnet-4-6`, `--max-turns 20`) to classify the failure (`actionable-bug \| transient-infra \| test-event \| info-maintenance`, rules applied in order so `factory.pilot.test`→test-event and `severity:info`→info-maintenance fire before anything else — the OIL-12/OIL-13 noise cases) with a confidence + root-cause, and posts that diagnosis as a comment back on the Linear issue via `commentCreate`. Investigation only: **no code changes, no PR, no issue-state change.** The OTel JSON and run logs are handed to the model as files framed as UNTRUSTED data (never executed — prompt-injection guard). All external calls (Linear, App-token, log fetch) are soft-fail. Secrets are pulled from Secret Manager at runtime and masked; none are committed. Also opens `DEVPLAN.md` (`status: active`, via `/dev-stage`) tracking the 7-stage loop (observe → suggest → approve → verified-close + docs); stage 1 marked `in-progress`. Security posture for the loop is locked to **two identities** (worker = `factory-master-broker`, approver = a minimal `oil-autofix-approver` App, prevent-self-review ON) — registered in a later stage. Verification = `workflow_dispatch` on OIL-16 after merge → a classification/root-cause comment appears on the issue; confirm no PR/code/state change. |

## Stage 116 — feat: /dev-stage — living DEVPLAN.md development mechanism (command + template + CI gate + docs)

| PR | Type | Summary |
|---|---|---|
| TBD | feat | New user-invoked `/dev-stage` flow: each new development runs as ordered, documented stages tracked in a living `DEVPLAN.md`, reported to the user in plain Hebrew on demand (the user never opens a file), and enforced by CI. Ships `templates/devplan/DEVPLAN.template.md` (plan template) + `.claude/commands/dev-stage.md` (the command, modeled on `handoff.md`, mirrored byte-identical into `templates/system/.claude/commands/` per the skills-mirror gate) + `scripts/check-devplan-updated.sh` with a `Check devplan updated` step in `changelog-check.yml` (twin of the CHANGELOG gate: fails the build when a root `DEVPLAN.md`/`devplans/*.md` is `status: active` and code changed without updating it in the same diff; no-op when no plan is active; detection path-scoped so the seed template never triggers it; no `on: paths:`, no new required check — lives in the existing `Changelog gates` job). Also scopes the CLAUDE.md "Never … session-summary files" rule with a narrow inline exception (original wording preserved verbatim) and documents the flow in CLAUDE.md "Development workflow" + `docs/roadmap.md`. No provisioning/deploy/skills/runtime logic touched. Verified locally: shellcheck + yamllint clean; the gate proven in 4 scenarios (no plan→PASS, plan updated→PASS, code-only→exit 1, completed→releases). |

## Stage 115 — feat: Phase F Gap 5 — real OpenRouter cost in spend_log via key-usage polling

| PR | Type | Summary |
|---|---|---|
| TBD | feature | Closes Gap 5 of `docs/phase-f-handoff.md` §2. `spend_log.cost_usd` was hardcoded `0` (only writer = `tg-proactive`'s "Log Spend" node), so the daily 24h spend summary always showed $0. The handoff's suggested fix — a Code node reading `$('OpenRouter Chat Model').last().json.usage` — is **infeasible**: n8n's `lmChatOpenRouter` sub-node `response.tokenUsage` is silently dropped and unreachable downstream (`"No path back to the referenced node"`; confirmed by n8n#26302 / n8n#21673 + docs), for both `chainLlm` and `agent` nodes. Instead, new `templates/system/workflows/n8n/spend-track.json` — an hourly (`0 * * * *`) Schedule-Trigger workflow — reads OpenRouter's authoritative `GET /api/v1/key` (auth via the existing `openRouterApi` credential as `predefinedCredentialType`; OpenRouter credits are 1:1 USD so `data.usage` is cumulative dollars; no management key — the inference key itself authenticates), computes the incremental delta against a new 1-row `spend_track_state` table, and `INSERT`s `('openrouter-key', delta)` into `spend_log` only when delta>0. First run seeds the baseline WITHOUT logging history; negative deltas clamp to 0 (month/key-reset guard); unreadable usage logs nothing and self-heals next run. `db-setup.json`'s "Create Tables" node gains `CREATE TABLE IF NOT EXISTS spend_track_state` (singleton `BOOLEAN` PK, `NUMERIC(12,6)`) + the table appended to the verification SELECT list. `tg-proactive.json` loses the hardcoded `cost_usd 0` "Log Spend" node + its connection (`Send Proactive` becomes terminal); its unchanged 24h `SUM(cost_usd)` reader now reflects REAL spend. `configure-agent-router.yml` gains a soft-fail install block (section 5e) that upserts+activates spend-track, gated on Postgres + OpenRouter (NOT Telegram/chat_id), plus a job-summary row. Idempotent + soft-fail (every PG/HTTP node `onError:continueRegularOutput`; a separate workflow sharing no nodes with the chat path → can never affect the bot). Per CLAUDE.md `templates/system/` edits reach only newly-provisioned systems; verification = re-run `configure-agent-router.yml` on `factory-test-tgbot16` (idempotent), trigger spend-track (seed → delta>0 after a real chat message), confirm a non-zero `openrouter-key` `cost_usd` row + tg-proactive `spend_24h>0` via `inspect_n8n_execution`. Auth fallback ready (a dedicated `httpHeaderAuth` Bearer credential) if `predefinedCredentialType:openRouterApi` 401s (n8n#19439/#26850). Gap 1 (HITL writes) is now the only remaining open Phase F item. |

## Stage 114 — docs: Phase F Gap 4 — AGENTS.md.template documents the Telegram chat bot

| PR | Type | Summary |
|---|---|---|
| TBD | docs | Closes Gap 4 of `docs/phase-f-handoff.md` §2. `templates/system/AGENTS.md.template` described the bot in one dense bullet; it now has a dedicated **Telegram Chat Bot** section matching what Stages 95–113 actually shipped: the two separate bots (chat `n8n-telegram-bot-token` via `tg-inbound`; alerts `telegram-bot-token` via `emit-event.sh`) sharing one `chat_id`; the inbound flow (Telegram → Caddy `telegram-in` exemption authed by `X-Telegram-Bot-Api-Secret-Token` → tg-inbound → router); the 5 Phase F workflows (`tg-inbound`, `tg-proactive`, `style-refresh`, `postgres-named-queries`, `db-setup`); the **7** Postgres tables with key columns (the handoff said 6, but `db-setup.json` creates 7: n8n_chat_histories, style_profile, audit_log, spend_log, tg_updates_seen, pending_actions, events); the read-only `postgres_named_query` 4-query whitelist + SYSTEM-INFO injection on `unknown-agent`+`ops-agent`; and the two deferred items (real `spend_log` cost = Gap 5, HITL writes = Gap 1). The dense bullet under "What was provisioned" is slimmed to a pointer, and one Forbidden-Actions bullet now forbids giving the bot free-SQL/write tools. Docs-only — no workflow/Caddy/script/runtime change; per handoff §3 rule 5 it reaches systems provisioned after merge, so no test-system provision was needed for a documentation change. Full Hebrew reference stays in `docs/telegram-chat-bot.md`. Gaps 1 and 5 remain open. |

## Stage 113 — fix: postgres_named_query — drop specifyInputSchema, trigger back to passthrough (proven on tgbot15 with the operator sending a real message)

| PR | Type | Summary |
|---|---|---|
| TBD | fix | Stages 110/111/112 all chased the wrong path: with `executeWorkflowTrigger` in `workflowInputs` mode + the `toolWorkflow` node carrying any flavour of `specifyInputSchema` (`manual` JSON Schema with enum, or `fromJson` example), n8n 1.121's bridge between LLM args and the trigger's `$json` shipped `query_name: null` regardless of what the LLM provided. Verified deterministically by two diagnostic rounds on `factory-test-tgbot14` (DEBUG1, after Stage 111's `workflowInputs`) and on `factory-test-tgbot15` (DEBUG2, after Stage 112's `schemaType:"fromJson"`): both surfaced identical evidence — `Keys at root: ["query_name"]`, `typeof query_name: object` (JS quirk: `typeof null === "object"`), `_raw_input: {"query_name": null}`. The schema → Zod → tool-args → trigger-pickBy pipeline in toolWorkflow v2.1 simply does not propagate the LLM's structured value to the sub-workflow. **DEBUG3 ran with `specifyInputSchema:false` (drop schema entirely) + trigger back to `passthrough`** — tgbot15 then produced a real bot reply ("your style profile is still empty"), and `inspect_n8n_execution` confirmed execution 28 terminates at **`Format Output`** (the success path), not `Unknown Query`. That's the first end-to-end success for the tool. Fix in main: (1) `templates/system/workflows/n8n/postgres-named-queries.json` — trigger back to `inputSource:"passthrough"` (the `workflowInputs` config was load-bearing only for Stages 110-112's schema dance, now reverted), Normalize Code node updated to extract `query_name` from `$json.query` as a raw string against the 4-value whitelist (also tries JSON.parse for safety). (2) `unknown-agent.json` + `ops-agent.json` — `postgres_named_query` toolWorkflow nodes lose `specifyInputSchema`, `schemaType`, `jsonSchemaExample`. Tool description rewritten to be unambiguous about the call shape ("Call this tool with ONE input — a single string that is EXACTLY one of these 4 query names, do not wrap it in JSON or any object, just send the raw string"). Inline code-comment on the Normalize node records the 3-stage diagnostic so a future reader sees why this specific shape was chosen. Per CLAUDE.md `templates/system/` edits reach only newly-provisioned systems; tgbot15's diagnostic copy is already shaped this way (the success was on tgbot15 *with* the diagnostic Telegram node — that node lives in tgbot15's own repo copy and won't migrate to other systems). Verification on a fresh `factory-test-tgbot16` after merge: 3 real Telegram messages, all 3 should produce success-path executions on `postgres-named-queries` (failingNode/lastNode = `Format Output`) and zero `Unknown Query` terminations. |

## Stage 112 — fix: postgres_named_query schemaType — Stage 111's diagnostic on tgbot14 proved the LLM was passing `query_name: null`

| PR | Type | Summary |
|---|---|---|
| TBD | fix | Stage 111's Normalize Code node + `_raw_input` debug fields wasn't surfaced by the LLM in its summary replies, so the diagnostic was inconclusive on `tgbot13`. Pushed a temporary diagnostic to `factory-test-tgbot14` (`Send Debug to Telegram` node after `Unknown Query`, hardcoded credential id `OIFaNWm8pQYzOj2s`) that **bypassed the LLM entirely** and surfaced the raw `$json` shape directly to the operator's Telegram. **Result**: `Keys at root: ["query_name"]`, `Item count: 1`, `_raw_input: {"query_name": null}` — the trigger IS exposing `query_name` at root level (Stage 110's `workflowInputs` fix was correct), but the **value arriving is `null`**, not the LLM's chosen string. Root cause: `toolWorkflow` v2.1 with `specifyInputSchema:true, schemaType:"manual"` and a JSON-Schema-with-enum-string. n8n's parser doesn't reliably convert string `enum` constraints to a Zod schema the LLM provider can act on — either the LLM never receives a useful schema (so it omits the parameter, and the trigger fills the missing field with `null` defaults from `workflowInputs.values=[{name:"query_name",type:"string"}]`), or n8n's manual-schema → Zod translation drops the enum's string values and substitutes null on validation. Fix: switch the two `postgres_named_query` `toolWorkflow` nodes (in `unknown-agent.json` and `ops-agent.json`) from `schemaType:"manual"` + JSON-Schema-string to `schemaType:"fromJson"` + a `jsonSchemaExample` literal (`{"query_name":"style_profile_get"}` in unknown, `{"query_name":"executions_summary_24h"}` in ops). The "fromJson" path is n8n's documented well-trodden flow — it derives the Zod schema from the example value, and the example serves as a worked illustration to the LLM of exactly what to pass. Tool description also tightened to be explicit: "**MUST** pass `query_name` as one of EXACTLY these 4 string values". No template change to `postgres-named-queries.json` itself — Stage 111's Normalize node stays in place and the trigger's `workflowInputs` stays at `query_name:string`. Per CLAUDE.md `templates/system/` edits reach only newly-provisioned systems; verification = a fresh `factory-test-tgbot15` (the tgbot14 diagnostic doesn't carry the fromJson fix — it lives in its own repo's copy of the template, frozen at provision time). |

> Older stages (Stages 95–111 archived 2026-05-28 in [`docs/changelog-archive/CHANGELOG-2026-05-28.md`](docs/changelog-archive/CHANGELOG-2026-05-28.md); Stage 94 and earlier in [`docs/changelog-archive/CHANGELOG.md`](docs/changelog-archive/CHANGELOG.md)).
