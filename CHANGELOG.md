# Changelog

## Stage 123 — chore: OIL auto-fix Stage 4 — revert blocked GitHub-environment gate, pivot to Telegram

| PR | Type | Summary |
|---|---|---|
| TBD | chore | Stage 122 built the approval gate as a GitHub **Environment** (`oil-autofix`) with a required-reviewer App + `prevent_self_review`. On merge, the auto-triggered `setup-oil-environment.yml` proved **live** that this is impossible on our plan: GitHub returned **HTTP 422** — environment protection rules (`required_reviewers`, `wait_timer`, `prevent_self_review`) are **Enterprise-only for private repositories**, and `edri2or` is on the **Team** plan. Confirmed unambiguously via the REST API (branch-policy PUT = 200, but `prevent_self_review` = 422 and `required_reviewers` = 422) and GitHub's own changelog/docs. This PR **reverts the blocked gate** and pivots the approval mechanism to **Telegram** (which also merges the original stages 4+5 into one): deletes `.github/workflows/setup-oil-environment.yml` (it can only ever 422); restores `oil-autofix-investigate.yml` to its clean end-of-Stage-3 shape — removes the `apply` + `gate_smoketest` jobs (both referenced the blocked `environment: oil-autofix`), the `mode` input, and the `investigate` job `outputs`, and restores `issue_id` to `required: true`. **Kept** the harmless `pr_number` capture in the open-PR step (it will feed the Telegram bridge). The empty `oil-autofix` environment GitHub auto-created during the failed run was already deleted via the API (HTTP 204) — no repo residue. **Why this matters:** the merged `apply` job would otherwise let GitHub auto-create an unprotected `oil-autofix` environment on the next draft PR and log "✅ gate approved" with no real approval — a misleading phantom gate. The new gate (separate `oil-autofix-approver` App merging only after a verified Telegram ✅) is the next step and begins with an operator-gated App registration. DEVPLAN: stages 4+5 merged into "Telegram approval bridge" (stage 4, in-progress); stages 6→5, 7→6, 8→7 renumbered. No production loop logic changed; nothing auto-merged. |

## Stage 122 — feat: OIL auto-fix Stage 4 — approval gate (oil-autofix environment + waiting `apply` job)

| PR | Type | Summary |
|---|---|---|
| TBD | feat | Stage 4 builds the **approval gate** for the OIL auto-fix loop — the code half; the new `oil-autofix-approver` App and the one-click reviewer wiring follow as operator-gated steps. New `.github/workflows/setup-oil-environment.yml` (post-merge helper, `push` on its own path + `workflow_dispatch`, **not** a required check) mints a short-lived broker-App token with `administration:write` scoped to `or-factory-master` and idempotently creates the GitHub Environment **`oil-autofix`**: `prevent_self_review: true`, no wait timer, and a `main`-only deployment branch policy (`custom_branch_policies` + a `main` policy POST). It deliberately does **not** add a reviewer — the REST `reviewers` field accepts only User/Team, so adding the `oil-autofix-approver` App as required reviewer is one operator click in repo Settings → Environments; the step reads the env back and prints whether that click is still pending. Idempotent: an existing environment is never re-PUT (so a UI-added reviewer is never clobbered), only its `main` policy is ensured, and it warns if `prevent_self_review` is somehow off. `oil-autofix-investigate.yml` gains: a `mode` input (`investigate` default / `smoketest`); job-level `outputs` on `investigate` (`pr_opened`/`pr_url`/`pr_number`, with the PR number now captured in the open-PR step); a new **`apply`** job (`needs: investigate`, `if: main && pr_opened=='true'`, `environment: oil-autofix`, `permissions: {}`) that pauses on the gate and — once approved — only records that the gate opened, containing **zero merge logic** (merge is Stage 5); and a tiny **`gate_smoketest`** job (`environment: oil-autofix`, runs only for `mode=smoketest`) that proves the pause with no AI / no PR / no GCP / no merge. The two identities stay separate (broker opens the PR; `oil-autofix-approver` approves) and `prevent_self_review` stops the broker approving its own run. Both workflows are `main`-pinned, all actions SHA-pinned. **Nothing is merged in this stage.** Verified pre-merge: `yamllint` clean across all workflows; embedded bash shellcheck-clean; YAML parses to the three expected jobs. Live gate-pause verification (`mode=smoketest`) runs post-merge, after the approver App + reviewer click. DEVPLAN stage 4 → in-progress. |

## Stage 121 — chore: OIL auto-fix Stage 3 — live draft-PR-path verification (verified; scaffold removed)

| PR | Type | Summary |
|---|---|---|
| TBD | chore | Live end-to-end verification of Stage 3's **draft-PR** outcome — the escalation path was already proven live on OIL-16, but the fixer *autonomously opening a DRAFT PR* had not been seen. Because `oil-autofix-investigate.yml` runs only on `main` (`if: github.ref == 'refs/heads/main'`) and checks out `main`, a reproducible bug had to sit on `main` during the run: a tiny **inert** fixture `scripts/oil-selftest-draftpr.sh` (`sum_two`, documented to sum but coded `a - b`) was merged — nothing referenced or executed it (`pipeline-tests.yml` only shellcheck-lints `scripts/*.sh`), so `main` stayed green — then a throwaway Linear issue (OIL-19, `action_required:false` so the live bell stayed quiet) drove the real investigator → fixer → deterministic gate. **Result (2026-05-29):** run 26612031701 escalated safely (the fixer's first candidate slipped → the safety net working), and the retry run 26612482036 produced a valid candidate → `VERDICT: open_pr` → the broker App **auto-opened DRAFT PR #171** (base `main`, `draft:true`, `a - b`→`a + b` + a fail-before/pass-after test under `scripts/tests/`), link posted to OIL-19. Teardown: PR #171 **closed without merging**, its branch deleted, OIL-19 canceled, and this PR removes the fixture — `main` returns to identical content. No production code/workflow changed; nothing was auto-merged. |

## Stage 120 — feat: OIL auto-fix Phase 1 / Stage 3 — guarded DRAFT-PR fixer

| PR | Type | Summary |
|---|---|---|
| TBD | feat | Stage 3 extends `oil-autofix-investigate.yml`: after the read-only diagnosis, when it is `actionable-bug` (confidence ≥0.8) in `or-factory-master`, a write-mode `claude-code-action` (Read/Grep/Glob/Edit/Write/MultiEdit — **no Bash, no web**) generates the smallest fix + a plain-`bash` repro test; the workflow (not the AI) then opens a **DRAFT** PR and posts the link to Linear. Anything else → escalate the diagnosis with a reason, no PR. New `scripts/oil-autofix-validate.sh` (shellcheck-linted; 6 local fixtures) is the deterministic gate: ≤2 files / ≤100 lines, forbidden paths (`.github/workflows|actions`, `terraform/`, `*wif*`/`*secret*`/`*creds*`/`*.pem`/`.env*`), a diff-scoped secret grep, and a test that MUST fail-before / pass-after, run under a scrubbed `env -i`. All GCP creds are revoked + the workspace creds file deleted **before** any AI-authored code runs; the candidate commit excludes `oil-context/` + `gha-creds-*.json`; the branch push + PR use a per-run broker-App token scoped to one repo (`contents`+`pull_requests` write, masked) while the job `GITHUB_TOKEN` stays `contents:read`. **DRAFT only — never merged** (merge = stages 4–5, a separate `oil-autofix-approver` identity). Each fix-path step is `continue-on-error` → a hiccup degrades to escalation. DEVPLAN stage 3 → completed; gate unit-tested locally, escalation path verified live on OIL-16 post-merge. |

## Stage 119 — fix: OIL auto-fix Phase 1 — activation (broker-Bot actor gate + idempotent webhook self-register)

| PR | Type | Summary |
|---|---|---|
| TBD | fix | One-time activation of the OIL auto-fix loop (DEVPLAN stage 2) plus the live verification deferred from PR 1 — which surfaced a real bug. **Investigator (`oil-autofix-investigate.yml`):** a live `workflow_dispatch` on OIL-16 (fired by the broker App via the factory `dispatch_workflow` tool) failed in ~25 s — `claude-code-action` rejects a non-human actor (`Workflow initiated by non-human actor: factory-master-broker (type: Bot)`). The loop is ALWAYS driven by the broker App — both the manual dispatch AND the production `repository_dispatch(oil-investigate)` the MCP fires after triage — so without this it could never run. Fix: `allowed_bots: "factory-master-broker,factory-master-broker[bot]"` (this one App, both login forms; never `*`; the run stays strictly read-only). **Webhook registration (`deploy-mcp-server.yml`):** the planned manual `curl POST /oil-register-webhook` can't run from the agent sandbox (egress reaches `api.github.com` but not `*.run.app`, and `probe_endpoint` can't send the `X-Admin-Secret` header). So registration is now a final, idempotent, `continue-on-error` deploy step that reads `mcp-server-admin-secret` from SM (`::add-mask::`ed) and POSTs the freshly-deployed region URL's `/oil-register-webhook` (`registerLinearWebhook` is idempotent → `created` once, `exists` after; only a non-secret webhook id/status is logged). Both workflows are `main`-pinned (`if: github.ref == 'refs/heads/main'` + the broker WIF CEL), so both fixes live-verify on the post-merge `main` run, not pre-merge. DEVPLAN stage 2 → completed. No provisioning/runtime logic touched. |

## Stage 118 — feat: OIL auto-fix Phase 1 / PR 2 — the Linear "bell" (webhook → rules triage → repository_dispatch)

| PR | Type | Summary |
|---|---|---|
| TBD | feat | Second PR of the OIL auto-fix loop — the inbound "bell" that turns a newly-opened `OIL-*` Linear issue into an automatic, read-only investigation. New `services/mcp-server/src/oil-autofix.ts`: `verifyLinearSignature` (constant-time HMAC-SHA256 over the RAW request body vs the `Linear-Signature` header), `extractOtel` (pulls the OTel JSON the observability pipeline embeds in the issue body — same fenced-block contract as PR 1), `triage` (deterministic rules applied IN ORDER so `factory.pilot.test`→test-event and `severity:info`→info-maintenance fire **before** `action_required` — exactly the OIL-12/OIL-13 noise cases — then `action_required:false`→skip, else dispatch), `handleLinearWebhook` (503 if disabled, 401 on bad signature, replay-guard on `webhookTimestamp`, only `action:create`+`type:Issue`, then triage → `repository_dispatch(oil-investigate)`), and `registerLinearWebhook` (idempotent Linear `webhookCreate` → `<mcp>/linear-webhook`). `index.ts` adds a raw-body capture to `express.json({verify})` and registers `POST /linear-webhook` (always 2xx unless the signature is bad, so Linear never retry-storms / auto-disables) + admin-gated `POST /oil-register-webhook`. `github-client.ts` gains `dispatchRepositoryEvent` (mirrors `dispatchWorkflow` — same broker-App `ghFetchRepo` auth, POSTs `/dispatches`, expects 204). `observability-client.ts` exports `readSecretSoft` + `linearGql` for reuse. `tools.ts` adds `oil-autofix-investigate.yml` to the `dispatch_workflow` allowlist (lets the agent run an on-demand verification; the live loop triggers via repository_dispatch). `deploy-mcp-server.yml` mints + mounts `LINEAR_WEBHOOK_SECRET=linear-webhook-secret:latest` (broker SA already holds project-level secretAccessor — no new IAM). The loop's own events (`factory.oil_autofix.{dispatched,skipped,dispatch_failed}`) are emitted soft-fail and **non-actionable** (info/warning, `action_required:false`) so it can never open a Linear issue about itself and re-trigger. Detection keys on the OTel-JSON signature (not the `auto-created` label, which Linear's payload doesn't reliably carry by name; label honoured when present). **Scope kept focused** vs the original plan: the reconciler backstop is split to a separate follow-up PR; Haiku triage is deferred (the investigator classifies deeply; rules suffice to drop noise); the payload JWT is deferred (repository_dispatch needs privileged creds + the investigator is read-only → low risk). DEVPLAN stage 1 → completed, stage 2 → in-progress. **Activation is one-time and operator-gated** (not in this PR's merge): redeploy the MCP via `deploy-mcp-server.yml` (self-mints the secret), then `POST /oil-register-webhook`, then live-verify by dispatching the investigator on OIL-16 + a benign test failure end-to-end — this also closes PR 1's deferred live check. Verified pre-merge: `tsc` typecheck clean; gate scripts + yamllint green. |

## Stage 117 — feat: OIL auto-fix Phase 1 / PR 1 — read-only investigator workflow + DEVPLAN opened

| PR | Type | Summary |
|---|---|---|
| TBD | feat | First PR of the OIL auto-fix loop — the "second half" of the observability pipeline (today it detects failures and opens `OIL-*` Linear issues, but nothing reads and fixes them). New `.github/workflows/oil-autofix-investigate.yml`: triggers on `repository_dispatch:[oil-investigate]` (the MCP server will fire this after triage in a later stage) and on manual `workflow_dispatch(issue_id)` for testing. It authenticates to the control project via the existing broker WIF chain (same pattern as `observability-pilot.yml`/`factory-health-audit.yml`), reads the target Linear issue (the `id` field accepts the `OIL-NN` identifier as well as the UUID), extracts the OTel JSON the pipeline wrote into the issue body, derives `factory.run_id`/`factory.layer`/`factory.system_name`, and best-effort fetches that run's logs with a tightly-scoped broker App token (`actions:read` on just the target repo — factory-layer runs in `or-factory-master`, system-layer runs in the system's own repo) via `scripts/generate-app-token.sh`. It then runs `anthropics/claude-code-action` pinned to the v1 commit SHA `787c5a0c` in strictly READ-ONLY mode (`--allowedTools Read,Grep,Glob --disallowedTools Bash,Edit,Write,MultiEdit,NotebookEdit,WebFetch,WebSearch`, `--model claude-sonnet-4-6`, `--max-turns 20`) to classify the failure (`actionable-bug \| transient-infra \| test-event \| info-maintenance`, rules applied in order so `factory.pilot.test`→test-event and `severity:info`→info-maintenance fire before anything else — the OIL-12/OIL-13 noise cases) with a confidence + root-cause, and posts that diagnosis as a comment back on the Linear issue via `commentCreate`. Investigation only: **no code changes, no PR, no issue-state change.** The OTel JSON and run logs are handed to the model as files framed as UNTRUSTED data (never executed — prompt-injection guard). All external calls (Linear, App-token, log fetch) are soft-fail. Secrets are pulled from Secret Manager at runtime and masked; none are committed. Also opens `DEVPLAN.md` (`status: active`, via `/dev-stage`) tracking the 7-stage loop (observe → suggest → approve → verified-close + docs); stage 1 marked `in-progress`. Security posture for the loop is locked to **two identities** (worker = `factory-master-broker`, approver = a minimal `oil-autofix-approver` App, prevent-self-review ON) — registered in a later stage. Verification = `workflow_dispatch` on OIL-16 after merge → a classification/root-cause comment appears on the issue; confirm no PR/code/state change. |

## Stage 116 — feat: /dev-stage — living DEVPLAN.md development mechanism (command + template + CI gate + docs)

| PR | Type | Summary |
|---|---|---|
| TBD | feat | New user-invoked `/dev-stage` flow: each new development runs as ordered, documented stages tracked in a living `DEVPLAN.md`, reported to the user in plain Hebrew on demand (the user never opens a file), and enforced by CI. Ships `templates/devplan/DEVPLAN.template.md` (plan template) + `.claude/commands/dev-stage.md` (the command, modeled on `handoff.md`, mirrored byte-identical into `templates/system/.claude/commands/` per the skills-mirror gate) + `scripts/check-devplan-updated.sh` with a `Check devplan updated` step in `changelog-check.yml` (twin of the CHANGELOG gate: fails the build when a root `DEVPLAN.md`/`devplans/*.md` is `status: active` and code changed without updating it in the same diff; no-op when no plan is active; detection path-scoped so the seed template never triggers it; no `on: paths:`, no new required check — lives in the existing `Changelog gates` job). Also scopes the CLAUDE.md "Never … session-summary files" rule with a narrow inline exception (original wording preserved verbatim) and documents the flow in CLAUDE.md "Development workflow" + `docs/roadmap.md`. No provisioning/deploy/skills/runtime logic touched. Verified locally: shellcheck + yamllint clean; the gate proven in 4 scenarios (no plan→PASS, plan updated→PASS, code-only→exit 1, completed→releases). |

