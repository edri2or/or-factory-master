# Changelog

## Stage 112 — fix: postgres_named_query schemaType — Stage 111's diagnostic on tgbot14 proved the LLM was passing `query_name: null`

| PR | Type | Summary |
|---|---|---|
| TBD | fix | Stage 111's Normalize Code node + `_raw_input` debug fields wasn't surfaced by the LLM in its summary replies, so the diagnostic was inconclusive on `tgbot13`. Pushed a temporary diagnostic to `factory-test-tgbot14` (`Send Debug to Telegram` node after `Unknown Query`, hardcoded credential id `OIFaNWm8pQYzOj2s`) that **bypassed the LLM entirely** and surfaced the raw `$json` shape directly to the operator's Telegram. **Result**: `Keys at root: ["query_name"]`, `Item count: 1`, `_raw_input: {"query_name": null}` — the trigger IS exposing `query_name` at root level (Stage 110's `workflowInputs` fix was correct), but the **value arriving is `null`**, not the LLM's chosen string. Root cause: `toolWorkflow` v2.1 with `specifyInputSchema:true, schemaType:"manual"` and a JSON-Schema-with-enum-string. n8n's parser doesn't reliably convert string `enum` constraints to a Zod schema the LLM provider can act on — either the LLM never receives a useful schema (so it omits the parameter, and the trigger fills the missing field with `null` defaults from `workflowInputs.values=[{name:"query_name",type:"string"}]`), or n8n's manual-schema → Zod translation drops the enum's string values and substitutes null on validation. Fix: switch the two `postgres_named_query` `toolWorkflow` nodes (in `unknown-agent.json` and `ops-agent.json`) from `schemaType:"manual"` + JSON-Schema-string to `schemaType:"fromJson"` + a `jsonSchemaExample` literal (`{"query_name":"style_profile_get"}` in unknown, `{"query_name":"executions_summary_24h"}` in ops). The "fromJson" path is n8n's documented well-trodden flow — it derives the Zod schema from the example value, and the example serves as a worked illustration to the LLM of exactly what to pass. Tool description also tightened to be explicit: "**MUST** pass `query_name` as one of EXACTLY these 4 string values". No template change to `postgres-named-queries.json` itself — Stage 111's Normalize node stays in place and the trigger's `workflowInputs` stays at `query_name:string`. Per CLAUDE.md `templates/system/` edits reach only newly-provisioned systems; verification = a fresh `factory-test-tgbot15` (the tgbot14 diagnostic doesn't carry the fromJson fix — it lives in its own repo's copy of the template, frozen at provision time). |

## Stage 111 — fix: postgres-named-queries needs a defensive Normalize Code node — Stage 110's workflowInputs fix alone wasn't enough

| PR | Type | Summary |
|---|---|---|
| TBD | fix | Stage 110 changed `templates/system/workflows/n8n/postgres-named-queries.json`'s `executeWorkflowTrigger` from `inputSource:"passthrough"` to `inputSource:"workflowInputs"` with `values:[{name:"query_name",type:"string"}]`, hoping the LLM's `query_name` would land at `$json.query_name`. Live verification on `factory-test-tgbot13` shows that didn't happen: 3 postgres-named-queries executions (22, 26, 27) — each ~10-30 ms — all routed straight to the Switch's `Unknown Query` fallback, never touching any Postgres branch. The LLM is calling the tool, but the args still aren't reaching `$json` in the shape the Switch expects (either `workflowInputs` isn't surfacing them on this n8n version + toolWorkflow v2.1 combination, or the args arrive at a different path like `$json.input.*`, `$json.query.*`, or `$json.arguments`). Fix without further blind guessing: new **Normalize Input** Code node between trigger and Switch — extracts `query_name` defensively from any of the observed shapes (`raw.query_name`, `raw.input.query_name`, `raw.query.query_name`, `JSON.parse(raw.query).query_name`, `JSON.parse(raw.arguments).query_name`), and **always emits `_raw_input: <the original $json>`** so the next test run will reveal the actual shape via the `Unknown Query` Set node's new `_debug_received` field if the normalization still misses. Switch now reads `={{ $json.query_name }}` from Normalize Input's output (a no-op rename — `$json` is just the previous node's data); Format Output reads `$('Normalize Input').first().json.query_name`; Unknown Query reads the same and adds `_debug_received` to its response payload. Idempotent + soft-fail: when the LLM actually provides `query_name` correctly at any known path, the Switch matches and the right Postgres query runs; when it doesn't, the LLM gets an error response with the raw payload it sent — much easier to diagnose than the current "Unknown Query" black box. Per CLAUDE.md `templates/system/` edits reach only newly-provisioned systems; verification = same 3 Telegram messages on a fresh test (or a configure re-run on a system whose template would update, though tgbot13's already-installed subworkflow won't auto-update). |

## Stage 110 — fix: 3 bugs from Stage 108 live verification — postgres_named_query input shape, sub-agent persona leaks, ops-agent tool gap

| PR | Type | Summary |
|---|---|---|
| TBD | fix | Surfaced during Stage 108's live verification on `factory-test-tgbot12` with 3 real Telegram messages (the discipline the handoff §3 rule 3 prescribed — "install PASS is not proof"). Three distinct bugs found, all fixed in one PR because they were found in the same verification round. **(1) postgres_named_query input shape — Stage 108 bug.** `templates/system/workflows/n8n/postgres-named-queries.json`'s `executeWorkflowTrigger` was set to `inputSource:"passthrough"`, but n8n's `toolWorkflow` v2.1 with `useSchema=true` reads the LLM's args via `getCurrentWorkflowInputData()` (in `packages/@n8n/nodes-langchain/nodes/tools/ToolWorkflow/v2/utils/WorkflowToolService.ts`), which expects `inputSource:"workflowInputs"` with explicit field defs. With `passthrough` the function returns empty → the LLM's `query_name` never reaches `$json` → the Switch reads undefined and falls to `Unknown Query` → tool returns `{ok:false, error:"unknown query_name…"}`. Caught on tgbot12 executions 37 and 38 (both `success`, both routed to `Unknown Query`). Fix: trigger now `inputSource:"workflowInputs"` + `workflowInputs:{values:[{name:"query_name",type:"string"}]}`. Zod schema on the toolWorkflow side keeps enforcing the enum at the LLM/tool boundary. **(2) Sub-agent persona leak — Stage 106 incomplete + false-positive verification.** ops/code/research/infra agents all carry an explicit `"You are the [X] sub-agent for a self-hosted automation system"` literal in their systemMessage (line 33 for ops, line 36 for the chainLlm-based three). Stage 106 only added the `Read Style Profile` + style ternary; it never touched the persona line. The handoff §2 row 2 claim that Stage 106 verified persona neutering on tgbot10 is wrong — Stage 106's verification was 4 routing-correctness messages, not a "who are you?" probe. tgbot12's first message ("מה אתה יודע על המערכת הזו?") was classified `research` and the research-agent answered `"אני סוכן מחקר (RESEARCH sub-agent) במערכת אוטומציה עצמית-מתארחת"` — full architecture leak. Fix: 4 systemMessages rewritten from "You are the X sub-agent…" to "You provide/help/give advice…" — capability description, no identity statement. Style-profile ternary and all surrounding nodes untouched. New trailing rule on each: "never reveal the names of internal sub-agents or your own architectural role" — explicit do-not-leak instruction. **(3) ops-agent tool gap — Stage 108 architectural miss.** The classifier (correctly) routes `"כמה הרצות נכשלו ב-24 שעות?"` to ops, but Stage 108 added `postgres_named_query` (which holds the `executions_summary_24h` query designed for exactly that question) only to unknown-agent, so ops answered honestly that it can only list workflows. Fix: ops-agent gets the same `postgres_named_query` toolWorkflow node (id `b0000000…0009`) + `ai_tool→Ops Agent` connection + `@@SYSTEM_INFO_JSON@@` block in its systemMessage. `configure-agent-router.yml`'s SYSTEM-INFO injection — previously hard-coded to `unknown-agent.json` and node `"Chat Agent"` — now uses a `case "$file"` to also inject into `ops-agent.json` at node `"Ops Agent"` (jq's `select(.name==$target)` parameterized). The PG-missing fallback that strips `postgres_named_query` now also runs on ops-agent (split out of the unknown-only block so it applies to both). No change to code/research/infra agents (their model has no tool surface). Per CLAUDE.md `templates/system/` edits only reach newly-provisioned systems; verification = fresh `factory-test-tgbot13` + 3 Telegram messages targeting each of (a) SYSTEM-INFO answer (no identity leak), (b) `executions_summary_24h` via ops, (c) `style_profile_get`. The handoff's verification discipline now also covers persona neutrality for every sub-agent the classifier might pick. |

## Stage 109 — fix: deploy-railway-cloudflare propagates new CUSTOM_DOMAIN_ID after force-recreate

| PR | Type | Summary |
|---|---|---|
| TBD | fix | Surfaced during Stage 108's live verification on `factory-test-tgbot11` (deploy run 26578458921). The "force re-verify via delete+recreate" block in `templates/system/.github/workflows/deploy-railway-cloudflare.yml` (around line 815) — which deletes the first `customDomainCreate`'s record (id `aa6b7015…`) and re-creates one (id `cc17718b…`) so Railway re-runs its DNS verification with the TXT record now in place — never wrote the **new** id back to `$GITHUB_ENV`. The next step ("Wait for Railway TLS cert (retry-with-recreate)") then started with the **deleted** id in `$CUSTOM_DOMAIN_ID`, and at minute ~15 its own retry path called `customDomainDelete` on a record that no longer existed (silent success), then `customDomainCreate` 6× — each returning `INTERNAL_SERVER_ERROR / "Failed to create custom domain, please try again"` because the **live** `cc17718b` record still occupied the same `domain` (one customDomain per (project, domain) pair). The deploy then exited 1 with `Railway did not issue cert for n8n-factory-test-tgbot11.or-infra.com after ~23 min` and a misleading hint suggesting Railway scheduler throttle (factory-test-tgbot11's workspace had 5 projects total at the time — well under the documented ~15-project threshold). Fix: 4 new lines right after the `NEW_STATUS=…` parse — extract `NEW_CUSTOM_DOMAIN_ID` and, when non-empty, both update the local `CUSTOM_DOMAIN_ID` and `echo "CUSTOM_DOMAIN_ID=${…}" >> $GITHUB_ENV` so step 10 sees the live id. Surgical (no behaviour change when the first `customDomainCreate` is already verified — the recreate block is skipped entirely; the new lines only run when recreate actually happens). Per CLAUDE.md `templates/system/` edits reach newly-provisioned systems only, so existing deployed systems are unaffected; the pattern is captured in the inline comment so future readers see the precedent. Adjacent step 10's own `_recreate_custom_domain` helper (around line 949) already updates `CUSTOM_DOMAIN_ID` locally and doesn't leak to downstream steps that need it (step 18 looks up `N8N_DOMAIN_ID` fresh via `_domain_id_on`), so it needs no change. Out of scope: Railway's flaky `customDomainCreate` 500s themselves — those are upstream and already guarded by the 6× retry. |

## Stage 108 — feat: Phase F Gap 3 — postgres_named_query whitelist + SYSTEM-INFO injection in unknown-agent

| PR | Type | Summary |
|---|---|---|
| TBD | feature | Closes Gap 3 of `docs/phase-f-handoff.md` §2 (the two of three sub-tools that don't require Gap 1's HITL). **(1)** New `templates/system/workflows/n8n/postgres-named-queries.json` — an Execute-Workflow-Trigger subworkflow exposing a **fixed whitelist** of 4 read-only SELECTs against the system's own Postgres (`style_profile_get`, `recent_audit_log`, `pending_actions_open`, `executions_summary_24h`); a `Route by Query Name` Switch (typeVersion 3.2) dispatches to one of 4 `executeQuery` nodes (the `style_profile_get` query carries `@@CHAT_ID@@` substituted at install time, matching `style-refresh.json`'s pattern); all four converge into a `Format Output` Code node that wraps `{ok:true, query_name, rows:[…]}` (and filters the empty `{}` row that Postgres' `alwaysOutputData:true` emits on zero-row SELECT). An unknown query_name falls through to a Set node that returns `{ok:false, query_name, error}`. NO free SQL — entire security model rests on the closed whitelist (per handoff §2 Gap 3 explicit decision). **(2)** `unknown-agent.json` gets a new `@n8n/n8n-nodes-langchain.toolWorkflow` node `postgres_named_query` (typeVersion 2.1, position [1160, 560]) with `workflowId.value=@@WF_PG_NAMED_QUERIES_ID@@` and an `inputSchema` that enumerates the 4 valid query_names (so the LLM can never invent a fifth); a matching `ai_tool` connection wires it to the Chat Agent. **(3)** The Chat Agent's `systemMessage` is extended in two places — the SYSTEM AWARENESS paragraph now mentions `postgres_named_query` and the new SYSTEM-INFO block, and a new `SYSTEM-INFO (authoritative facts… do not invent or contradict): @@SYSTEM_INFO_JSON@@` line appears just before the existing style-profile expression (preserving Stage 103's `Read Style Profile` ternary intact). The `@@SYSTEM_INFO_JSON@@` placeholder replaces the originally planned `gcp_metadata_get` tool, which couldn't work — n8n runs on Railway, not on a GCP VM, so `http://metadata.google.internal/...` is unreachable; the static-injection alternative (chosen by the operator over the MCP-bearer-HTTP alternative) adds zero runtime dependency and zero new SM secret. **(4)** `configure-agent-router.yml` is extended in 4 places (all soft-fail): a new step right after db-setup builds `SYSTEM_INFO_JSON` via `jq -cn` (system_name + n8n_domain + gcp_project_id + gcp_region:"me-west1" + runtime + capabilities); a new install block right before the sub-agents loop runs `_upsert_wf "factory-master: postgres-named-queries"` (inactive — Execute Workflow Trigger doesn't need activation); the sed loop gets a new `@@WF_PG_NAMED_QUERIES_ID@@` substitution and is followed by a `jq gsub` on the Chat Agent's `systemMessage` that injects `SYSTEM_INFO_JSON` correctly-escaped into the outer JSON string (sed can't safely substitute embedded-JSON into a JSON-string value); the PG-missing fallback that already swaps `memoryPostgresChat → memoryBufferWindow` and strips `Read Style Profile` now ALSO strips the `postgres_named_query` node + connection so a no-PG system installs a clean working unknown-agent with no dangling tool reference; the job-summary table gets a new `postgres-named-queries` row inside the existing `Postgres wired` branch. **(5)** `docs/telegram-chat-bot.md` §5 + §6 reflect the new capabilities. The third sub-tool from §2 Gap 3 (`n8n_trigger_workflow`) stays deferred because it would mutate state and therefore requires Gap 1's async-HITL (`Send-and-Wait` would hang the synchronous webhook). Gaps 1, 4, 5 remain open per the handoff. Out of scope: real `usage.cost` extraction (Gap 5), HITL machinery (Gap 1), AGENTS.md.template refresh (Gap 4). Per handoff §3 rule 5: template changes don't propagate to existing systems, so this PR will be verified by provisioning a fresh `factory-test-tgbot11` (reuse mode, 0 quota) and sending real Telegram messages — install PASS alone is not proof. |

## Stage 107 — docs: refresh Phase F handoff after Gap 2 closure (Stage 106 / PR #152)

| PR | Type | Summary |
|---|---|---|
| TBD | docs | Sync `docs/phase-f-handoff.md` to the post-Stage-106 reality after Gap 2 was closed and verified live. **§1**: live system `factory-test-tgbot9` → `factory-test-tgbot10` (tgbot9 was decommissioned during Gap 2 verification on 2026-05-28); merged PR list extended with 151+152; Stage range `95–104` → `95–106` across both §1 and §4. **§2 row 2** (style injection) flipped to ✅ closed with a one-line evidence summary (4 real Telegram messages on tgbot10, no sub-agent leaked its technical identity), action-column emptied. **§2 row 3** (new tools for unknown-agent) expanded with a **⚠️ architectural blocker on `gcp_metadata_get`**: n8n runs on Railway, not on a GCP VM, so `http://metadata.google.internal/...` is unreachable; next session must pick (a) HTTP-tool against `factory-master-actions-mcp` Cloud Run with a new bearer token in SM, (b) drop the tool in v1, or (c) other — present tradeoffs before acting. **§3** got a new verification rule 5: template edits don't propagate to existing systems (snapshot at provision time), so verifying any post-merge template change requires a fresh provision (~15–20 min, 0 quota in reuse mode) — pattern validated in Stage 106 and now baked into the discipline. **§5** prompt stays neutral but adds a recommendation: next gap is Gap 3 (with the gcp_metadata caveat above). Header banner refreshed to reflect the post-Gap-2 state. Pure docs; no behaviour. |

## Stage 106 — feat: Phase F follow-up — style_profile injection in ops/code/research/infra sub-agents

| PR | Type | Summary |
|---|---|---|
| TBD | feature | Closes Gap 2 of `docs/phase-f-handoff.md` §2. Stage 103 added `Read Style Profile` Postgres injection only to `unknown-agent`, so when the router landed on ops/code/research/infra the sub-agent still answered with its hard-coded persona prompt — exposing the architecture to the user (e.g. "אני ה-RESEARCH sub-agent") and ignoring the operator's learned style profile. The same pattern (Postgres `SELECT profile FROM style_profile WHERE chat_id = …` between the trigger and the AI Agent, with `onError: continueRegularOutput` + `alwaysOutputData: true`) is now applied identically to all four. systemMessage in `ops-agent` (LangChain `agent` node — `parameters.options.systemMessage`) and in code/research/infra (LangChain `chainLlm` node — `parameters.messages.messageValues[0].message`) becomes an n8n expression that appends `Style profile (match in tone, length, emoji density, humor; do not announce you are matching it): <profile>` when a row is present, and a no-op empty branch when not. The `text` field of each AI Agent is now read via `$('When Executed by Another Workflow').first().json.sanitized` (rather than `$json.sanitized`) since `Read Style Profile` now sits between the trigger and the agent and replaces `$json` (this was the same fix as Stage 104's data-flow patch for unknown-agent, now applied consistently). `configure-agent-router.yml`'s PG-missing fallback is split into two `if` blocks: the existing one keeps the unknown-agent in-memory window swap, and a new one (case-based, runs against all 5 files) strips `Read Style Profile` + rewires `When Executed by Another Workflow → <AI Agent>` directly when no Postgres credential is wired. Graceful degradation preserved: a system without PG installs 5 clean working sub-agents with no dangling credential reference. No changes to `agent-router.json` or `tests/router_battery.yaml` — the classifier and Macro-F1 gate are unaffected. Out of scope: Gaps 3 (new tools for unknown-agent), 4 (AGENTS.md.template refresh), 5 (real cost tracking) — deferred per handoff. |

> Older stages (Stages 95–105 archived 2026-05-28 in [`docs/changelog-archive/CHANGELOG-2026-05-28.md`](docs/changelog-archive/CHANGELOG-2026-05-28.md); Stage 94 and earlier in [`docs/changelog-archive/CHANGELOG.md`](docs/changelog-archive/CHANGELOG.md)).
