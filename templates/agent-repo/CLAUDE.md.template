@AGENTS.md

<!-- Claude Code reads this file natively. AGENTS.md is the cross-tool standard
     (Cursor, Codex, Copilot CLI, Gemini CLI, etc.) — imported above so both
     ecosystems read the same content. Edit templates/agent-repo/AGENTS.md.template
     in edri2or/or-factory-master, never this file directly. -->
