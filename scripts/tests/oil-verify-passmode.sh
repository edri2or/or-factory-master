#!/usr/bin/env bash
# oil-verify-passmode.sh — a trivially PASSING reproducer fixture (OIL Stage 5),
# the success-path companion to oil-verify-failmode.sh. Used to exercise the OIL
# verify SUCCESS path (VERDICT: verified) — e.g. the Stage-5 live verification, or
# a future on-demand check. Always exits 0.
echo "oil-verify-passmode: passing fixture (Stage 5 success-path)."
exit 0
