- factory-dismantle batch 4 (step 1 — repoint before repo retirement): the two dead standing/proving-system repos `or-edri-4` + `or-edri-base` are being retired (their GCP + Railway were already deleted in earlier phases). Before deleting `or-edri-4`, repointed every **live** default that still named it to the surviving system `or-aios`, so the kept Google-health machinery keeps working: `deploy-mcp-server.yml` post-deploy `smoke_target` (input default + description + the `|| 'or-aios'` runtime fallback), `google-mcp-smoke.yml` `system` default, `workspace-token-audit.yml` `SMOKE_SYSTEM` (the daily heartbeat — most critical), and `publish-static-site.yml` `source_repo` default. This also makes the monitoring test the real live system instead of a dead one. The actual repo deletion (both repos) is step 2, Telegram-gated via `propose-repo-delete.yml`. The OIL-approver App and agent-repo code leftovers were found to be load-bearing for kept flows and are deferred to batch 5; dead E2E config (`e2e-surfaces.json` / `check-e2e-proof.sh` / `e2e-proofs/`) is toothless (its gate workflow was deleted in batch 1) and also deferred to batch 5. `scripts/runtime-audit-targets.sh` still names `or-edri-4` but has no live workflow consumer — left for the batch-5 dead-code sweep.
