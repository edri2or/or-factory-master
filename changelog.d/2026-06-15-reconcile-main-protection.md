## fix: remove leftover classic branch protection on main (reconcile to ruleset-only)

| Type | Summary |
|---|---|
| fix | `or-factory-master/main` carried a leftover **classic** branch protection (`strict: true` + the original 4 contexts + `enforce_admins`, applied by a 2026-05-29 one-shot, run `26634127083`) coexisting with the `protect-main` ruleset (non-strict, 6 contexts). GitHub enforced both, so the classic `strict: true` silently forced "branch up-to-date before merge", overriding the ruleset's non-strict intent — it blocked the "behind" PR #471 with `405 4 of 4 required status checks are expected`. `scripts/ensure-protect-main-ruleset.sh` now deletes any leftover classic branch protection (new `_api_code` status-only helper; `DELETE /branches/main/protection`) immediately after it confirms the ruleset is active — idempotent (`404` = already clean), non-fatal, and never before the ruleset is in place, so the branch is never left unprotected. The obsolete `set-factory-branch-protection.yml` one-shot is deleted. The same cleanup self-heals every pre-2026-06 system the next time it is hardened. Self-applies on merge (the `protect-main.yml` path-filter covers the script); verified post-merge via the run log + a confirming `GET → 404`. |
