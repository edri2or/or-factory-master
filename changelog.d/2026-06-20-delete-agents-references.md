- **Deleted the agent roster + scrubbed its live references (`delete-agents-references`).** At Or's request, removed the named agent-repos — the coordinator/CEO `nuriel`, the first-wave workers `nachshon` / `natan-research` / `sapi-docs`, the write-capable `agent-builder`, and the builder target `personal-life` — and every *current-state* mention that could mislead a later session into thinking they exist. The 6 repos themselves are deleted via the Telegram-gated bridge (`propose-repo-delete.yml`, correlation `del-agents-20260620`), verified to 404 separately. **Surgical, not a teardown:** the generic agent-repo machinery stays, dormant — the broker (`agent-action.yml`), provisioners (`provision-agent-repo.yml` / `refresh-agent-repo.yml`), templates (`templates/agent-repo*`), the risk classifier (`scripts/agent-classify.sh`), and the operator-only coordinator MCP route (`coordinator-scope.ts`) — with all named values emptied to fail-closed: `COORDINATOR_REQUESTER_REPOS` / `COORDINATOR_WORKER_REPOS` (`deploy-mcp-server.yml`) = `""`, and `worker_capabilities` / `builder_allowed_targets` / `always_red_workers` (`policy/agent-risk-tiers.yml`) emptied. Deleted the persona docs (`docs/agent-specs/{nuriel,firstwave}/**`) and the agent capability cards (`docs/capability-cards/{nuriel-orchestration,firstwave-fanout,agent-repo-skills,agent-broker-handoff}.md`); rewrote `CLAUDE.md` § "Agent-repos" into "a dormant capability, no live agents" (+ how to bring agents back). Replaced agent names with generic placeholders in `coordinator-scope.ts`, `index.ts`, the MCP tests, the classifier self-test (now driven by a new `tests/agent-classify-policy.yml` fixture so the capability-cap stays covered), the smoke defaults, and both `templates/agent-repo*/AGENTS.md.template`; refreshed the agent-repo golden (`tests/golden/agent-repo`). **History kept:** dated `devplans/*` and `changelog.d/*` are untouched, and the 3 remaining `nuriel`/`nuriel-coordinator` mentions in `CLAUDE.md` + `docs/mcp-connector-setup.md` are explicitly labeled historical. Also dropped a stray committed `scripts/__pycache__/*.pyc` and added `__pycache__/` to `.gitignore`. Proven green: MCP tests 138/138 (tsc clean), classifier self-test 8/8, extractor bats 4/4, shellcheck `--severity=error` + yamllint + golden all pass.
