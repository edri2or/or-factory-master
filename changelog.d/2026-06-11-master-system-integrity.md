# Changelog fragment — master-system-integrity (2026-06-11)

> A per-development changelog fragment (date + slug), written here instead of at the head of
> `CHANGELOG.md` per the repo's default. The **Compile changelog** workflow folds it into a
> numbered `CHANGELOG.md`. The CI changelog gate accepts this fragment.

## fix: agent correctness + self-awareness — real Google account in the ops prompt, 8 named queries documented, fictional-account CI guard (master-system-integrity Stage 2, PR 1)

| Type | Summary |
|---|---|
| fix | First fix-cluster of the `master-system-integrity` development (verify every dev actually works on or-edri-4 + is embedded in the factory). **(1) Google account:** `templates/system/workflows/n8n/ops-agent.json` told the model to call `google_workspace` with `user_google_email="shared-google@or-infra.com"` — a **fictional** account (docs/google-identities.md) that the rebuilt workspace-mcp gateway **rejects**; every system born after google-wallet-unify carried this, so Gmail/Calendar failed even though the gateway was already correct (`services/workspace-mcp/entrypoint.sh:19` / `render-mcp-service-yaml.sh:34` both default to the real `edriorp38@or-infra.com`). Fixed the prompt to `edriorp38@or-infra.com`. **(2) Self-awareness:** `templates/system/AGENTS.md.template` documented only **four** `postgres_named_query` SELECTs and "7 Postgres tables" — stale since bot-trace-observability; a Claude session on a new system therefore did not know it could read the bot's own conversation. Now lists all **eight** named queries and adds a "Reading this bot's own conversation" note (`conversation_transcript` = word-for-word transcript; `tool_trace_recent`/`claim_actual_mismatch` = the per-tool attempt→result black box) reachable via the `postgres_named_query` tool or the system's `/mcp/system-tools` MCP endpoint; "7" → "10" tables (single Create-Tables node). **(3) Regression guard:** `scripts/check-golden-sync.sh` gains invariant (C) — fails CI if `shared-google@or-infra.com` reappears anywhere under `templates/system/` (the string had resurrected twice). Golden refreshed via `scripts/check-system-golden.sh --update` (one manifest line + rendered AGENTS.md). Template-only — reaches systems provisioned after merge; or-edri-4 gets it live via `refresh-system-agents.yml` + configure (master-system-integrity Stage 2/3, Or-gated). |
