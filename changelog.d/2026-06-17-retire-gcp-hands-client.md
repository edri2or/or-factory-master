# Changelog fragment — retire-gcp-hands-client (2026-06-17)

> Per-development changelog fragment. Folded into `CHANGELOG.md` with running Stage numbers by
> `scripts/compile-changelog.sh`.

## fix: retire the dead `gcp-hands-client` skill from the system template

| Type | Summary |
|---|---|
| fix | Removes the dead `gcp-hands-client` Claude skill that every provisioned system was still born with. The repo it dispatched to — `edri2or/gcp-hands` — no longer exists (consolidated into `or-factory-master` as `gcp-action.yml` + the MCP Telegram approval bridge during 2026-06-06..10, control project deleted, repo archived then deleted; a live API check now returns 404). The skill still described `edri2or/gcp-hands` as a live cross-repo `repository_dispatch` (type `gcp-action`) target whose results return as GitHub issue comments via a `gcp-hands-broker-app` token — a flow that no longer exists — and a research session reading `templates/system/**` had it auto-surface and cited it as a live production precedent. **Changes:** (1) deletes `templates/system/.claude/skills/gcp-hands-client/{SKILL.md,README.md}`; (2) rewrites the three references that routed to it to the honest current model — `operate-this-system/SKILL.md` (systems have no interactive GCP path: GCP work happens only inside the system's own Actions workflows via its WIF, and a system *requests* a resource from the broker via `scripts/emit-event.sh` rather than dispatching), `github-app-operations/SKILL.md` (the "never fetch the private key outside Actions" guardrail is preserved; only the dead issue-comment-leak rationale is replaced with the generic "any interactive surface can leak it"), and `AGENTS.md.template` (drops the `(incl. gcp-hands-client)` parenthetical); (3) removes the hard-require in `provision-system.yml` that FAILED provisioning if the skill file was missing (the whole `.claude` tree is copied wholesale), and fixes its explanatory comment + success-summary line; (4) refreshes `tests/golden/system/**` (`scripts/check-system-golden.sh --update`) so the byte-exact golden no longer fingerprints the two dead files. Template-only — new systems are born without the dead skill; existing systems' backfill is a separate Or-gated follow-up (refresh copies, it does not delete). No new capability; the E2E proof gate does not apply (no bot-behavior files touched). Historical/consolidation references to gcp-hands (`gcp-action.yml`, `gcp-classify.sh`, `archive-old-repos.yml`, devplan, prior changelog fragments) are correct and left untouched. |
