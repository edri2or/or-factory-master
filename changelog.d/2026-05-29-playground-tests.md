## feat(ci): Playground Tests — runtime-validation CI layer (dev-stage)

**Overview.** Adds a fifth CI gate — **Playground tests** — that validates script
*logic* and *template rendering* at runtime, complementing the four existing
static gates (changelog, shellcheck + yamllint, secret-scan, supply-chain) which
only check syntax. This PR ships stages 1–6 (BATS infra, 5 per-script test files,
the template-render validator, the CI workflow, and the dev-stage wiring). Stage 7
(adding `Playground tests` as a required status check in the protect-main ruleset)
ships in a **separate PR** after this merges and the workflow runs green on `main`
at least once — so the required context exists before it's required. Tracked in
`devplans/playground-tests.md`.

| Type | Summary |
|---|---|
| feat | **Stage 1 — BATS infrastructure.** Add `bats-core` (v1.13.0), `bats-support` (v0.3.0), and `bats-assert` (v2.2.4) as git submodules under `scripts/tests/bats/` and `scripts/tests/test_helper/{bats-support,bats-assert}/` — each pinned to a full SHA, no `apt`-installed bats. Add the shared helper `scripts/tests/test_helper/common.bash` (anchored absolute paths so any `.bats` file under `scripts/tests/` can `load test_helper/common`): loads bats-support + bats-assert, exports `REPO_ROOT`, and provides `make_tmpdir` / `make_fixture_repo` / `common_teardown` for tests that need throwaway git repos or temp dirs. `scripts/tests/_smoke.bats` proves the harness end-to-end (5 checks, all PASS). Lays the ground for stage 2's per-script tests and stage 4's CI workflow. Devplan: `devplans/playground-tests.md`. |
| feat | **Stage 2 — BATS tests for 5 core scripts.** Add `scripts/tests/lib.bats` (4 cases — `get_code_files` filter), `scripts/tests/check-changelog-updated.bats` (5 cases — fixture git repos with/without changelog touch, docs-only no-op), `scripts/tests/check-devplan-updated.bats` (5 cases — no-plan/completed/active × updated/not, two-active-plans), `scripts/tests/check-actions-pinned.bats` (5 cases — reusable-workflow callers; surfaces a **latent bug**: the script's regex `^\s+uses:` skips the standard `      - uses:` step form, so step-level action uses are not inspected today; the tests document actual behavior, fix is deferred to a separate dev-stage per the "do not modify existing 4 gates in this PR" constraint), `scripts/tests/check-workflow-permissions.bats` (4 cases — scoped permissions PASS, top-level + indented `write-all` FAIL). All tests use the stage-1 harness; **`bats scripts/tests/*.bats` = 28/28 PASS** locally. No network or cloud creds needed. |
| feat | **Stage 3 — Template rendering validation.** Add `scripts/tests/validate-templates.sh` — recursively finds every `*.template` under `templates/system/`, renders each with `envsubst` using the **same 14-variable allow-list** `.github/workflows/provision-system.yml` uses (`SYSTEM_NAME`, `GCP_PROJECT`, `PROJECT_NUMBER`, `ISO_TIMESTAMP`, `PUBLIC_URL`, `HEALTH_URL`, `REPO_URL`, `GITHUB_RUN_ID`, `GITHUB_RUN_URL`, `WIF_PROVIDER`, `MODE`, `GENERIC_SECRETS`, `SYSTEM_PURPOSE`, `AGENT_NAME` — kept in sync deliberately, single source of truth), then asserts the rendered output is clean: (1) **no leftover `${VAR}` or `$VAR`** placeholders (a misspelled `${SYSTEM_NAEM}` would slip past raw-file linting and only blow up at provision time on a billed GCP project — this check catches it), (2) `*.yml.template`/`*.yaml.template` rendered output passes yamllint when available, (3) `*.sh.template` rendered output passes `bash -n` syntax check. Exits 0 on clean run with named PASS lines; exits 1 on any failure with the failing template named and the leftover placeholders listed. Today's two templates (`AGENTS.md.template`, `CLAUDE.md.template`) PASS; a temporary `${UNDEFINED_VAR}` insertion was verified to FAIL with a clear message. |
| feat | **Stage 5 — dev-stage.md Playground verification step.** Add step **(a.1) Verify via Playground** to Step 3's execution loop in `.claude/commands/dev-stage.md` (between (a) Implement and (b) Update bookkeeping): after implementing a stage, push and wait for CI, confirm the "Playground tests" check is green before bookkeeping, and if Playground isn't relevant (docs-only stage) record "Playground: N/A — no testable changes" and proceed. Add **Safety Rule #7**: never proceed to bookkeeping with a red Playground check unless the stage is docs-only/non-testable. Mirrored byte-identically into `templates/system/.claude/commands/dev-stage.md` (the two have been kept in sync since the parallel-dev-stage work); the N/A clause makes the step self-correcting for provisioned systems, which don't carry the factory-internal `playground-tests.yml`. |
| feat | **Stage 4 — Playground Tests CI workflow.** Add `.github/workflows/playground-tests.yml` — the fifth required CI gate. Runs on every PR (and push to main) with `permissions: contents: read` only. Steps: (1) `actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd` (same SHA the repo already uses, `submodules: true` for bats-core, `fetch-depth: 0` for git-history-aware tests), (2) install `actionlint` v1.7.7 via the rhysd-published release tarball, (3) install `gettext-base` + `yamllint` (validate-templates uses both), (4) `actionlint -shellcheck "shellcheck -S error"` — same severity floor `pipeline-tests.yml` already uses for raw shellcheck, so we don't suddenly start failing the build on the info/style hits the repo deliberately ignores; clean on all current workflows, (5) `bats scripts/tests/*.bats` — 28 cases from stage 2, (6) `bash scripts/tests/validate-templates.sh` — the template renderer from stage 3. Job name pinned to **`Playground tests`** — that's the exact string `scripts/ensure-protect-main-ruleset.sh` will require in the follow-up stage-7 PR. No new actions beyond the pinned `actions/checkout`. |
