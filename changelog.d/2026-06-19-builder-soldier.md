- **חייל-הבנאי (the builder-soldier) — נפתח הפיתוח (`builder-soldier`).** הוקמה תוכנית-פיתוח מדורגת (`devplans/builder-soldier.md`) להוספת חייל בעל-יכולת-כתיבה לצוות-הסוכנים, שבונה קוד לתוך `edri2or/personal-life` דרך הברוקר בלבד. **תיקון-עיגון מהותי מול הבלופרינט של נוריאל** (`edri2or/nuriel/results/builder-{docs-v1,blueprint-v4}.json`): הבלופרינט הניח worker עם מפתח-כתיבה עצמאי (GitHub App ייעודי + SA חדש), מה שהיה פורץ דלת שנייה בקיר-האבטחה של הפקטורי. העיצוב המעוגן שומר על החוק "רק הברוקר כותב; הכל דרך הדלת האחת" — ה-worker רק *מציע* קבצים (ארגז-חול, בלי טוקן), והברוקר פותח PR-טיוטה עם טוקן מצומצם ל-personal-life, בשער RED→טלגרם. תוצאה: בלי App/SA/WIF חדשים. 4 נקודות-הלברור הוכרעו (SA משותף; audit ב-Axiom+PR; בלם 50-קבצים; allowlist 3-שכבתי אצל הברוקר). **שלב 0** (הוכחת-יכולת) ✅ GO — ייצור-scaffold בארגז-חול + פתיחת-PR ע"י הברוקר (PR #540) + חסימת-בידוד מול ריפו אחר; הוכחת-בידוד ברמת-טוקן-ה-App נעוצה כשער-הכניסה של שלב 2 על main. **שלב 1** ✅ — `policy/agent-risk-tiers.yml` (worker `agent-builder: write`, `builder_allowed_targets`, `builder_limits` 50/256KB, `always_red_workers`) + `scripts/agent-classify.sh` (override always-red → כל ריצת-בנאי נכפית ל-RED→טלגרם) + 2 fixtures (8/8 עוברים). **שלב 2** ✅ (קוד) — `scripts/builder-apply.sh` (validate: allowlist L1 + בלם 50/256KB + דחיית path-traversal/.git/ריק; apply: ענף `builder/<corr>` → push קבצים עם trailer `[builder-agent]` → **DRAFT PR**, idempotent) + הברוקר `agent-action.yml` (input `dry_run`, זיהוי builder-mode לפי `.target_repo`+`dl/out/`, מינוף טוקן מצומצם `{contents,pull_requests}:write` ליעד ב-execute, preview ללא-PR ב-dry_run). DRY_RUN מוכח offline; הברוקר עבר `bash -n`+`shellcheck -S error`. **שלב 3** ✅ — `templates/agent-repo-builder/**` (worker עם `Read,Grep,Glob,Write,Edit`, בלי Bash/טוקן, כותב הצעות ל-`result/out/` + מניפסט `result/<corr>.json`; persona-בנאי ב-AGENTS.md עם allowlist personal-life בלבד) + `provision-agent-repo.yml` input `template_dir` (enum fail-closed); golden של `templates/agent-repo/` לא נגע. עדיין לא נוצרה תשתית חיה (ריפואים/redeploy) — אלה שלבים 4–7, מאחורי שער-טלגרם; ההוכחה החיה של ה-apply היא שלב 6.
- **חייל-הבנאי — שלבים 4–5 (תשתית חיה, מאחורי שער-Or).** **שלב 4** ✅ — הוקם ריפו-הבנאי `edri2or/agent-builder` (פרטי) דרך `provision-agent-repo.yml` (`template_dir=templates/agent-repo-builder`, run 27849594236); אומת: AGENTS.md החי = אישיות-הבנאי (allowlist `personal-life` 3-שכבתי, בלי טוקן), כולל WIF-bind משותף. **שלב 5** ✅ — נוצר ריפו-היעד `edri2or/personal-life` (פרטי) ונועל ב-ruleset `protect-main`. תוך כדי כך תוקן `scripts/ensure-protect-main-ruleset.sh`: כשרשימת-ההקשרים ריקה (`REQUIRED_CONTEXTS_JSON='[]'`, ריפו בלי CI כמו personal-life) — חוק `required_status_checks` **מושמט** לגמרי, במקום להישלח עם רשימה ריקה (שגרם ל-HTTP 4xx, ואילו אם היה מתקבל היה תוקע את הריפו: בדיקה שלא רצה לעולם לא עוברת). תואם-אחורה: קוראים קיימים (factory + systems) מעבירים רשימה מלאה ומקבלים את אותה התנהגות.
