## feat: on-demand agent-exerciser + live n8n automation audit (n8n-agent-audit)

| Type | Summary |
|---|---|
| feat | New `.github/workflows/exercise-agent.yml` — on-demand behavioral probe that drives a REAL custom message through a live system's secured inbound (telegram-in → Caddy → tg-inbound → agent-router → routed specialist) via WIF + Secret Manager, reusing `scripts/e2e-verify-inbound.sh` with a custom `PROBE_TEXT`, and prints the agent's actual reply. Unlike `e2e-verify.yml` it writes no proof and commits nothing — a pure "does this agent actually answer?" tool to PROVE any agent (code/infra/research/deep-research) end-to-end or reproduce a silent failure. Pinned actions, `permissions:{}` + job `id-token:write`, `if: refs/heads/main`. |
| docs | Live audit of all 25 n8n workflows on or-edri-4 (`devplans/n8n-agent-audit.md`): core conversational path + tools + voice/vision + write-approval + maintenance are proven; `tg-proactive` and `style-refresh` fail silently daily (`column "<chat_id>" does not exist`; LLM "Bad request"); `code-agent`/`infra-agent`/`deep-research` had zero executions; `DB Vacuum` is healthy but its weekly schedule hasn't fired yet. |
| fix | `exercise-agent.yml` job grants `contents: read` so `actions/checkout` works — a job-level `permissions:` block defaults all unlisted scopes to `none`, so `id-token: write` alone produced `Repository not found` at checkout (caught on the workflow's first live run). |
| fix | `tg-proactive.json` proactive worker — drop `JSON.stringify('@@CHAT_ID@@')` in the `total_messages` subquery (it produced a double-quoted `"<chat_id>"` that Postgres read as a column → `column "<chat_id>" does not exist`); use the proven `'tg:' \|\| {{ '@@CHAT_ID@@' }}` pattern. Fixes the daily 08:00 failure. |
| fix | `style-refresh.json` style-learning worker — guard the "Extract Style" prompt against an empty transcript (`(no recent messages)` fallback) and add `onError: continueRegularOutput` so an LLM 400 falls through to the default-profile path instead of crashing. Fixes the daily 03:00 `Bad request` failure. |
| feat | New `.github/workflows/trigger-system-workflow.yml` — on-demand runner for a system's n8n workflow (the n8n public API can't execute, so it logs in with the system's owner creds via WIF+SM and calls the internal Execute-Workflow endpoint, then reads the resulting execution's real status from the public API). Lets the factory verify SCHEDULED workers live without waiting for cron — used to prove the tg-proactive/style-refresh fixes. |
