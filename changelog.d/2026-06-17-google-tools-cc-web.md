- **Google Workspace tools from Claude Code on the web — diagnosed + documented (`google-tools-cc-web`).** The Workspace connector (`/workspace/<system>/mcp`) already surfaces and works in a Claude Code **web** session — proven live 2026-06-17 (`search_drive_files` returned real Drive files from `edri2or@gmail.com`, no localhost OAuth). The reported failure (an OAuth prompt to `http://localhost:3002/oauth2callback`) is **not** a broken connector: it is a wrong `user_google_email`. The single-user `workspace-mcp` sidecar files the shared token under the label `edriorp38@or-infra.com`; any other value (e.g. `edri2or@gmail.com`) finds no credential and falls back to the sidecar's internal localhost OAuth. Fix is usage clarity, not a new connector. Mapped the failure → label in `.claude/commands/google-workspace-guide.md` (› "Known failure", `audience: shared` → mirrored to systems), `docs/mcp-connector-setup.md` (› "Claude Code on the web"), and `CLAUDE.md` § Web-session connector gate. No gateway/OAuth code or the four-site scope contract touched.
