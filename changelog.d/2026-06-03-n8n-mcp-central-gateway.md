## feat: central multi-tenant n8n-mcp gateway — live n8n workflow development (code-ready)

| Type | Summary |
|---|---|
| feat | New live-write path on the existing `factory-master-actions-mcp` Cloud Run service: `/n8n/<system>/mcp` reverse-proxies the MCP protocol to an internal `czlonkowski/n8n-mcp` **sidecar** (`>=2.51.2`, `MCP_MODE=http`, `ENABLE_MULTI_TENANT=true`, localhost-only — never publicly reachable), injecting `x-n8n-url`/`x-n8n-key` **server-side** from the system's own `n8n-api-key` (read cross-project via the existing `n8n-client.ts` path — the key never enters the Claude session). Per-tenant isolation is defence-in-depth: the system is in the URL path AND bound into a signed bearer (`bearer.ts` gains a `'n8n-dev'` kind + `system` claim; `/n8n/<system>/mcp` 403s on path↔claim mismatch). Live writes are scratch-only — `devNameViolation` refuses create/update of any non-`dev-` workflow, so git stays the source of truth (the commit-side gate `check-no-dev-workflows-committed.sh` in each system is the second half). New module `src/n8n-mcp-proxy.ts` + `resolveN8nTarget` in `src/n8n-client.ts`; `/n8n/<system>/token` mints a system-scoped admin-gated bearer. `deploy-mcp-server.yml` becomes a multi-container deploy (gateway + n8n-mcp sidecar) and mints `n8n-mcp-internal-auth-token` (≥32 chars, self-heal pattern). v1 is single-tenant (allowlist = `or-adhd-agent`). Unit-tested (`test/n8n-mcp-proxy.test.mjs`, the scratch-only + allowlist guards). **Code-ready, not deployed** — the Cloud Run sidecar + redeploy is Or-gated. Tracked in `devplans/n8n-mcp-central-gateway.md`. |
