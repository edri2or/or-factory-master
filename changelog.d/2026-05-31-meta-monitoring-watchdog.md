## feat: שומר-העל — שלב 1 (יסוד + workflows מתוזמנים + שער-CI + dead-man's-switch)

| Type | Summary |
|---|---|
| feat | Meta-monitoring watchdog stage 1: `monitoring/watchdog-registry.json` source-of-truth catalog + `scripts/run-watchdog.sh` (gh-run-freshness proof, 2-consecutive-failures rule, Hebrew Telegram report with direct evidence links) + `meta-monitoring-watchdog.yml` (daily 05:00 UTC) proving the 4 scheduled factory workflows ran + emitting `factory.watchdog.{ok,degraded}`. |
| feat | CI registry gate `scripts/check-watchdog-registry-updated.sh` (twin of `check-devplan-updated.sh`, wired into the `Changelog gates` job) blocks adding/removing a workflow/n8n/hook surface without updating the registry; `monitoring/registry-exempt.txt` allowlists deliberately-unmonitored one-shots. |
| feat | External dead-man's-switch `scripts/create-watchdog-heartbeat.sh` (Better Stack heartbeat, idempotent, URL stored in SM `watchdog-heartbeat-url`); the watchdog pings it every run so its own silent death is caught externally. |
| feat | Allowlisted `meta-monitoring-watchdog.yml` on the MCP `dispatch_workflow` tool (`services/mcp-server/src/tools.ts`) so the agent can run the one-time `setup_heartbeat=true` heartbeat setup + ad-hoc watchdog runs autonomously (no operator click). Cron operation is unchanged; the only write the dispatch enables is the guarded one-time SM `watchdog-heartbeat-url` write. |
| feat | Stage 2 — CI-gate coverage: new `gh-branch-protection` proof method in `scripts/run-watchdog.sh` confirms each gate's check `context` is still required by the `protect-main` ruleset (`GET /repos/.../rules/branches/main`) AND its latest run on `main` is green (a context dropped from branch protection is 🚨 even if the workflow file still exists). Registers the 5 required contexts (`Changelog gates`, `shellcheck + yamllint`, `Scan for committed secrets`, `Supply chain gates`, `Playground tests`) as `ci-gate-*` entries; bumps default `CURRENT_STAGE` to 2; adds 4 bats cases (ok / context-dropped→red / run-failed→red / no-runs→unknown). |
| feat | Stage 3 — hooks + event-driven workflows: two new proof methods in `scripts/run-watchdog.sh`. `static-integrity` (hooks) checks the script exists, is executable, and is still wired in its registration point (`.claude/settings.json`) — a wired-out hook is 🚨. `gh-last-run` (event-driven workflows: push/repository_dispatch/PR — no cron, no freshness window) requires the latest completed run on `main` to be green; a never-run workflow is ❓ not 🚨. Registers 2 hook entries + 5 event-workflow entries (`protect-main`, `oil-autofix-verify`, `oil-autofix-investigate`, `deploy-mcp-server`, `eval-agent-router`); exempts the PR-only `eval-agent-router-precheck.yml` in `monitoring/registry-exempt.txt`. Bumps default `CURRENT_STAGE` to 3; adds 8 bats cases; documents both methods in `monitoring/README.md`. |
| feat | Stage 4 — n8n per-system coverage + report provenance. New `n8n-execution` proof method in `scripts/run-watchdog.sh`: a DYNAMIC fan-out (like `system-runtime-audit.yml`) that enumerates real systems (own GCP project under the Systems folder; shared `factory-test-25` skipped), reads each system's `n8n-api-key` from its SM, and queries the n8n Public API (`GET /api/v1/executions?limit=1`, header `X-N8N-API-KEY`) — latest execution `success`→✅, `error`/`crashed`→🚨, none/no-key/not-deployed→❓ (never 🚨), 0 systems→❓. Adds the `system-n8n-executions` registry entry and bumps default `CURRENT_STAGE` to 4. Every report (Telegram + step-summary) now ends with a **provenance** line — the watchdog's own run URL + commit SHA — so a green report is traceable to the exact run. Adds 6 bats cases; documents in `monitoring/README.md`. Per-system ✅/🚨 verifies E2E only once a real system is deployed (none exist yet → the n8n line reads ❓). |
| fix | Watchdog proof methods no longer treat a GitHub `skipped`/`neutral` run conclusion as a failure (new `_conclusion_is_failing` helper, applied to `gh-last-run`, `gh-branch-protection`, and `gh-run-freshness` in `scripts/run-watchdog.sh`). Caught by the first real stage-3 run: `oil-autofix-verify` runs on every push to `main` but legitimately *skips* unless the commit is an OIL merge — its healthy consecutive `skipped` no-ops were being flagged 🚨 (false `factory.watchdog.degraded`). Adds 3 bats cases (skipped→ok for last-run + branch-protection; skipped doesn't mask a real failure pair). |
