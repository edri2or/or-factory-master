# Changelog fragment — system-operate-skill (2026-06-15)

> Per-development changelog fragment. Folded into `CHANGELOG.md` with running Stage numbers by
> `scripts/compile-changelog.sh`.

## feat: system-operate-skill — every provisioned system is born with an "operate-this-system" Claude skill (Stage 1)

| Type | Summary |
|---|---|
| feat | Stage 1 of system-operate-skill. Adds a new **static** Claude skill `templates/system/.claude/skills/operate-this-system/SKILL.md` so every system the factory provisions is born with a dedicated skill that lets a Claude Code session working in the system's repo recognise it as an or-factory-master system and operate it. The skill is a **map, not a manual**: it carries no live values and routes to the system's own `AGENTS.md` (the provision-time orientation doc) for each concern — `## Identity`, `## Telegram Chat Bot` › `### System-aware tools (read-only)`, `### Postgres tables`, `## Secrets in GCP Secret Manager`, `## GitHub App`, `## Built-in MCP connections`, `## Forbidden Actions` — and to the read-only state surfaces (`.mcp.json` servers `factory`/`n8n-live`, and the system's own `/mcp/system-tools` Bearer endpoint), plus the non-negotiable invariants (writes are HITL via `request_write_action`; secret values never printed; GitHub via the `github-app-operations` skill; GCP via the `gcp-hands-client` skill; `main` is PR-only). Three production-correctness choices: the filename is **uppercase `SKILL.md`** (a lowercase `skill.md` loads silently-nothing on Linux), the skill name is the **fixed** `operate-this-system` (= the directory name, contains no "claude"/"anthropic", no trailing/double hyphen → valid under the skill-name rules; per-system uniqueness comes from the `AGENTS.md` it points to, not the name), and the file is **100% static** — no `${VAR}`/`@@TOKEN@@`, because nothing substitutes placeholders under `.claude/skills/`. Frontmatter is kept minimal-portable (`name` + `description` only; the Experimental, SDK-unsupported `allowed-tools` is omitted for a pure routing skill); `description` is third-person "what + when" with triggers, ≤1024 chars. Propagation is automatic — `provision-system.yml` copies the whole `.claude` tree with a filter-free `cp -r`, and the skill is fingerprinted into the golden by `render-system-golden.sh`'s `find . -type f`; the `tests/golden/system/` golden is refreshed in this same PR (`scripts/check-system-golden.sh --update`) so the golden-sync twin gate stays green. Existing systems are backfilled in a later stage via `refresh-system-agents.yml` (`paths=.claude/skills/operate-this-system`); the E2E gate does not apply (a `.claude/skills/` path is not a bot-behavior file). Template-only — no factory runtime touched. |
