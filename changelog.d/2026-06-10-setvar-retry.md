## חיסון הקמה מול 401 לסירוגין של GitHub (setvar-retry)

- `provision-system.yml` / צעד `Set repo variables`: ‏`_set_var` עטוף עכשיו ב-retry
  (עד 5 ניסיונות, ‏5 שניות בין ניסיונות). ‏GitHub החזיר היום ‏401 לסירוגין לטוקן תקין
  (ריצות 27288991036 ו-27289578027 נפלו בדיוק שם — ‏201 ואז ‏401 באותה שנייה, אותו
  טוקן) — וזה היה צבר-הכתיבות היחיד ל-GitHub בוורקפלו בלי ניסיון-חוזר. קונפליקט
  ‏409/422 בניסיון חוזר נחשב הצלחה (ה-POST הקודם נחת לפני התשובה שהתפספסה).
  שתי מערכות-טסט יתומות מהכשל (factory-test-049/050 — ריפו בלבד, בלי Railway/DNS)
  יאורכבו בפירוק של פיתוח n8n-2x-upgrade.
