- factory-dismantle batch 7 (docs): rewrote `CLAUDE.md` and `README.md` as a folded single-system (or-aios support) operating manual — kept the Or-persona section verbatim, reframed the repo around the gateway + backbone + Google path + CI hygiene + `/dev-stage`, and removed all provisioning/template/agent-repo/OIL/E2E/golden framing. Deleted 12 dead factory docs (`e2e-enforcement-standard`, `e2e-verification-gate`, `oil-autofix`, `agent-repo-product`, `doc-drift-prevention`, `skills-audience`, `live-test-loop`, `master-integrity-matrix`, `roadmap`, `phase-f-handoff`, `reliability-layer`, `telegram-chat-bot`). Kept `capability-first` + `agent-isolation-testing` (still referenced by the shared `/dev-stage` command) and repaired 4 dangling cross-links in kept docs.
- Removed the orphaned `E2E verification gate` from the `protect-main` required-status-checks set in `scripts/ensure-protect-main-ruleset.sh`. The E2E workflow was deleted in batch 1 but the ruleset still required its context, so it could never report — leaving every PR `mergeable_state: blocked` and mergeable only via admin-bypass. The required set is now the 5 live gates (Changelog gates / shellcheck + yamllint / Scan for committed secrets / Supply chain gates / Playground tests). `protect-main.yml` re-applies the ruleset automatically on merge.
