# Changelog fragment — email-form-intake (2026-06-15)

> A per-development changelog fragment (date + slug), written here instead of at the head of
> `CHANGELOG.md` per the repo's default for all developments. The **Compile changelog** workflow
> folds it into a numbered `CHANGELOG.md` in one single-threaded run. The CI changelog gate accepts
> this fragment.

## feat: email-form-intake — Gmail→form→Telegram proposal workflow shipped into every system (Stage 1/4)

| Type | Summary |
|---|---|
| feat | Stage 1 of the email-form-intake development. New `templates/system/workflows/n8n/email-form-intake.json` (17 nodes) — a standalone background workflow the factory installs into every provisioned system: a daily Schedule Trigger + a Manual Trigger scan Gmail for emails with an attached form (PDF), read each PDF with the **proven multimodal OpenRouter pipeline** (native-PDF `file` content part → `google/gemini-2.5-flash` primary, `anthropic/claude-sonnet-4.5` fallback, `onError: continueErrorOutput`), and in a **PROPOSAL-ONLY** approach (never writes back onto the PDF) extract the form fields, propose per-field values (`source: fixed\|inferred\|missing` + confidence + evidence quote), compose a Hebrew proposal card via `chainLlm`+`lmChatOpenRouter`, run the tg-vision egress validator, and send it to Telegram for the operator. Gmail is read via **HTTP Request → Gmail REST API** with `nodeCredentialType: googleOAuth2Api`, reusing the existing "Google OAuth2 API" credential **without any scope change** — a deliberate divergence from the original plan's Gmail-node approach (the n8n Gmail node requires the single-service `gmailOAuth2` credential type, which `bootstrap-gmail-oauth.yml` does not create; the generic `googleOAuth2Api` is documented for exactly this HTTP-Request use). No custom Docker / pdf-parse / pdf-lib and no third-party PDF/OCR service (the form ID is sensitive — `mistral-ocr` is intentionally avoided). No real PII is committed (the fixed-details profile ships empty; supplied only at runtime). A paired capability skill (`templates/system/.claude/skills/email-form-intake/SKILL.md`, generated from a new `scripts/gen-workflow-skill.sh` table row) keeps the workflow↔skill gate green; `tests/golden/system/MANIFEST.sha256` refreshed; and the new workflow is listed in `monitoring/registry-exempt.txt` (provision-only daily worker — covered collectively by `system-n8n-executions`; intentionally not in the per-workflow `system-n8n-cadence` dead-man map yet, so it won't false-alarm on systems that predate it). Static gates verified locally (jq valid JSON, all 6 Code nodes pass `node --check`, workflow↔skill pair gate PASS). Live proof on `or-edri-4` + the signed E2E proof land in Stage 3. |

## feat: email-form-intake — wire into configure-agent-router (install + activate, graceful skip) (Stage 2/4)

| Type | Summary |
|---|---|
| feat | Stage 2 of the email-form-intake development. `templates/system/.github/workflows/configure-agent-router.yml` now installs + activates the workflow on every system: a new lookup-by-name resolves the existing "Google OAuth2 API" (`googleOAuth2Api`) credential → `GMAIL_OAUTH_CRED_ID` (created by the separate `bootstrap-gmail-oauth.yml`; absent ⇒ skip), and a new **"5d-bis"** block (mirroring the tg-proactive block) sed-substitutes the 5 placeholders, imports + publishes via `_upsert_wf` (the 2.x cookie-auth publish path that registers production routes), and reports it in the install summary table. **Graceful degradation:** imported + activated ONLY when the Gmail OAuth credential + OpenRouter + Telegram + chat id are ALL present; otherwise it logs INFO/WARN and skips, so a system that has not bootstrapped Gmail never gets a broken active workflow. `tests/golden/system/MANIFEST.sha256` refreshed (configure-agent-router.yml hash moved). Verified locally: yamllint clean; a dry-run of the exact `sed` leaves zero `@@…@@` placeholders and yields valid JSON with the credentials substituted. Live import + functional proof on `or-edri-4` land in Stage 3. |

## fix: bootstrap-gmail-oauth — store a real access_token + expiry so the credential works via HTTP OAuth2 (Stage 3, live-found)

| Type | Summary |
|---|---|
| fix | Stage 3 live-proof finding on `or-edri-4`. `templates/system/.github/workflows/bootstrap-gmail-oauth.yml` stored the auto-connected "Google OAuth2 API" credential with `oauthTokenData.access_token:""` + `expiry_date:0`. Because `expiry_date:0` is falsy, n8n deemed the empty token valid forever and never refreshed it — so the first live `email-form-intake` run (or-edri-4 execution 644) failed at the **List Form Emails** node with `NodeApiError: Unable to sign without access token`. A latent bug: no workflow had used this credential before, so it was never exercised. Fix: capture the freshly-minted `access_token` + `expires_in` from the refresh-token validation already performed, and store the credential **already-connected** — a real `access_token` and a real `expiry_date` (ms since epoch) — so n8n signs with it immediately and refreshes from the `refresh_token` on expiry (standard OAuth2). No scope change. `tests/golden/system/MANIFEST.sha256` refreshed. Re-applied to `or-edri-4` and re-verified live (functional proof + the signed E2E proof). |
