## fix: deploy converges in one run — Railway cert recreate + Caddy smoke race

| Type | Summary |
|---|---|
| fix | `templates/system/.github/workflows/deploy-railway-cloudflare.yml` — two flakiness fixes so a fresh system's deploy reaches live n8n in a **single run** (factory-test-83 needed 3). **(1) Railway TLS cert:** the "force re-verify via delete+recreate" deletes the custom domain then recreates it, but `customDomainCreate` flakes with `INTERNAL_SERVER_ERROR` for minutes; a single attempt (step 9) / 6×10 s≈1 min (step 10) left the domain **deleted-without-recreate**, wasting the 15-min cert poll on a domain that no longer existed (run 26660480702). Both recreate sites now wait 30 s after delete and retry patiently (10 attempts × 30 s ≈ 5 min) to outlast the 500, and step 9 **fails fast** (`exit 1`, re-dispatch) instead of silently proceeding with no domain. **(2) Caddy smoke test:** the unsigned `/webhook` → 401 check was a single shot right after `/health` went 200, but `/webhook/*` warms a beat later (briefly 404) → false failure; it now polls until 401 (up to ~3 min), mirroring the `/health` and HMAC polls. Template-only change → applies to systems provisioned after this; already-deployed systems unaffected. |
